# Connection audit (lead-owned; this version is the fresh end-of-mission Opus audit at ba15aaf, 2026-09-21; prior register kept as CONNECTIONS-2026-09-21-pre-audit.md)

Statuses: VERIFIED_LOCAL · VERIFIED_LIVE · FAILED · BLOCKED_EXTERNAL · NOT_IMPLEMENTED

# Connections audit — end-of-mission 36-connection pass (opus-auditor)

Model (self-reported from my system prompt): Opus 5 (claude-opus-5)

**Starting hash (main pass):** `origin/main` = `ba15aaf3addd7ddcbfbfa3fd2f85f32cb55243e1` (`ba15aaf`, "docs(reviews): T23 Phase 4 verification"). Local checkout HEAD was `a955010` (D125 docs, unpushed at start; docs-only). `convex/**` at `ba15aaf` is byte-identical to `529f545` (T23's candidate) — `git diff 529f545 ba15aaf -- convex` is empty; `src/**` differs from `529f545` only by the T24d frontend commit, and `ba15aaf` vs `86020f0` (the HEAD T23's browser suite actually ran against) is docs-only. So every T23 backend/browser observation cited below applies to `ba15aaf` unchanged.

**Delta pass:** see §7 at the end — every row states which hash its evidence refers to; rows re-checked against the delta are marked.

**Method (read-only on production):** detached worktree `/private/tmp/recoup-audit-ba15aaf` at `ba15aaf` (no `.env.local` copied; removed after). `npm ci` (Node v25.2.1 locally; CI pins 22 via `.nvmrc`) → `verify:patch` OK → `npx vitest run`: **65 files, 1287 passed + 1 expected fail (`fairness.test.ts:220`, F-D74-1)**, 42.5 s. GitHub Actions run **35625965752** on `ba15aaf` (push to `main`, 2026-09-21T16:29Z): `checks` job green (`npm ci`, `verify:patch`, typecheck, lint, `test:ci` "1288 tests passed across 65 file(s)", build); `codegen:check` **SKIPPED** ("no reachable Convex deployment is configured"); `e2e` job skipped the Playwright run (E2E_CONVEX_URL / E2E_DEPLOY_KEY not configured). Adversarial repros: `scratchpad/audit/repros/zz_audit.test.ts` (10 cases) and `zz_audit_board.test.ts` (2 cases) run against the worktree; results per row below. Two repro cases FAIL as designed (each is a finding: one already conditioned in D124/B4, one new).

Status vocabulary: VERIFIED_LOCAL · VERIFIED_LIVE · FAILED · BLOCKED_EXTERNAL · NOT_IMPLEMENTED. VERIFIED_LIVE is used only where T23 (`docs/reviews/phase4-verification.md`, dated 2026-09-21, deployment `adorable-lion-138`) or another dated log on a named deployment shows a live run **of the candidate**. The collaborator's I7a log (`docs/plans/2026-09-20-recoup-iterations.md:115`, `cool-oyster-399`, revision `d88c5f8`) is a real dated live run, but of a revision 1,242 insertions behind the candidate in the very files it exercised (`git diff --stat d88c5f8 ba15aaf -- convex/{auth,drafts,http,inbound,notify,profiles,replies}.ts`); it is recorded as historical evidence, not as the candidate's status.

---

## 1. Register (C01–C36)

Columns: re-derived producer → consumer → contract; auth at every hop / idempotency & failure / boundedness; proving tests (file:test) or NONE; status with the hash the evidence refers to; where the register (`docs/team/CONNECTIONS.md`) disagrees.

| ID | Producer → consumer → contract (re-derived from code) | Auth · idempotency/failure · bounds | Tests | Status @hash | Register disagreement |
|---|---|---|---|---|---|
| C01 | `.env.local:VITE_CONVEX_URL` → `src/main.tsx` (`isValidConvexUrl`: must be `https://`, else renders `ConfigMissing`, never constructs the client) → `ConvexReactClient` → `ConvexAuthProvider`. `VITE_CONVEX_SITE_URL` is written but unread. | n/a · a missing/invalid URL is a static page, not a crash · n/a | NONE automated for `main.tsx`; `scripts/smoke.mjs` check 4 ("exactly one `.convex.cloud` host") exists but FAILS on `adorable-lion-138` because no static build is deployed there (T23 §2). Build gate PASS (CI 35625965752). | VERIFIED_LOCAL @ba15aaf (code + build). Bundle-host check live: not runnable until a static deploy exists. | Agree. |
| C02 | `convex/auth.ts` `guardedPassword` (normalize → `authAttempt` limit → signUp global/per-address limits + `authMailGlobal` peek → password-format → `accountExists` (F2) → library `authorize`) → error mapping to 4 fixed strings; `callbacks.beforeSessionCreation` throws `WRONG_CREDENTIALS_MESSAGE` for tombstoned users; `http.ts:auth.addHttpRoutes` (OIDC discovery/JWKS) → `App.tsx` `Authenticated/Unauthenticated/AuthLoading` → `SignIn.tsx`. Mandatory verification (`verify: authMail("verify")`, D66). | session JWT; every non-enumerating path re-derived (signUp-as-oracle F2, lockout N1, unknown-address reset phantom-consume F3, format-before-probe N5) · rate-limited per address (10/10 min) and global (200/h) · bounded | `convex/authFlow.test.ts` (31), `convex/auth.test.ts` (16), `convex/lib/authMail.test.ts`, `lib/email.test.ts`; audit repro `zz_audit.test.ts › C02 sign-in` (signUp with victim's correct password → `ACCOUNT_EXISTS`; unverified signIn → `tokens: null`; verify via captured code → tokens; tombstoned signIn with right AND wrong password → byte-identical `WRONG_CREDENTIALS`) PASS. Live: T23 §2 check 6 `GET /.well-known/openid-configuration` 200 on `adorable-lion-138`; T23 §4 `auth.spec.ts` 10 pass ×2 projects (real Convex Auth on the dev deployment, codes via `testing:lastCodeFor`, `E2E_SEED_ENABLED=true`). | **VERIFIED_LIVE** @ba15aaf on `adorable-lion-138` for sign-up → code → verify → sign-in with the E2E transport; **BLOCKED_EXTERNAL** for a real verification email (`AGENTMAIL_API_KEY` placeholder, `ALERTS_INBOX_ID` absent by name, real sends not authorized — T23 §5). | Register says "VERIFIED_LOCAL; live sign-up/sign-in on dev only via testing.ts seed": consistent; the T23 e2e run upgrades the dev portion to VERIFIED_LIVE (dev). |
| C03 | `lib/access.ts:requireUserId` = `getAuthUserId` + `isTombstoned` (one indexed read on `accountState.by_user`), then `ownedPurchase/Item/Claim/Policy/Draft/Watch` (`doc.userId === userId` or "not found"). 41/57 public functions use it; of the 14 `getAuthUserId`-branch functions, 9 queries now call `isTombstoned` themselves (insights ×4, watches.list/get, tracking.overview, notify.drops, offers.listForWatch — verified in code at ba15aaf), the 4 actions (`drafts.generate`, `intake.paste`, `policies.refresh`, `profiles.ensureInbox`) resolve through a `requireActiveUserId` internalQuery = `requireUserId`, and only `profiles.me` (D124 LOW), `account.deletionStatus` and `account.requestDeletion` (both by design) remain ungated. | yes · throws `ConvexError` (same message for missing and foreign) · one read | `convex/boundary.test.ts` (65: second user supplies A's ids to every public fn; unauthenticated matrix; secret-substring leak check), `claims.test.ts › user B cannot read or mutate user A's claim`, `watches.test.ts › hides a watch from another user and refuses every write`, `market.test.ts › throws for a non-owner`, `priceWatch.test.ts › refuses an item belonging to another user`; audit repro `spend gates` PASS. Live: T23 §4 `isolation.spec.ts` 4 pass ×2. | **VERIFIED_LIVE** @ba15aaf (dev e2e isolation) / VERIFIED_LOCAL for the full matrix. | **Register says NOT_IMPLEMENTED — wrong.** Implemented since T04 and proven by boundary.test (65) + isolation e2e. |
| C04 | Settings/Composer `useAction(api.profiles.ensureInbox)` → `internal.profiles.requireActiveUserId` (tombstone-aware) → `byUser` → `createInboxRemote()` (raw `fetch POST /inboxes`, bearer by name, body never echoed) → `internal.profiles.save` (first row wins). Webhook routing resolves `profiles.by_inbox`. | requireUserId via `runQuery` · `save` idempotent per user; losing race orphans a live remote inbox (documented) · **not rate-limited: `rateLimits.ts:inboxProvision` is defined and never called** | `convex/profiles.test.ts` (6, incl. "provisions nothing for a tombstoned account: no fetch, no profile row"). Audit repro `zz_audit.test.ts › C04 ensureInbox`: **FAILS as designed** — 5 concurrent calls by one user → 5 `POST /inboxes`, 1 profile row, 4 orphaned provider inboxes (finding F-AUD-2). | VERIFIED_LOCAL @ba15aaf with finding F-AUD-2; live provisioning for the candidate **BLOCKED_EXTERNAL** (`AGENTMAIL_API_KEY` is a placeholder on `adorable-lion-138`; production not authorized). B3 (`save` ungated) open at ba15aaf → T18.5. | Register says VERIFIED_LIVE (cool-oyster-399, their I7a) — that is `d88c5f8`, not the candidate (`profiles.ts` +19/-… since). **Status changed → VERIFIED_LOCAL + BLOCKED_EXTERNAL (live).** |
| C05 | AgentMail → `POST /agentmail/webhook` (`http.ts`): env-name check for `AGENTMAIL_WEBHOOK_SECRET` (401 empty body if unset, F-T22-1) → component `handleWebhook` (svix verify with all three headers; 401 on failure normalized to empty body; any throw → `sanitizeError` → 401) → component `lib.handleEvent` (dedupe on `events.by_eventId`, stores raw) → callbackPool → `internal.inbound.onMessageReceived` (second dedupe on `processedEvents.by_external = event_id`; inbox → profile → `userId` patched first (6b-7) → tombstone → per-inbox 60/h → reply vs intake; never throws). | signature at hop 1; ownership re-checked at every routing step (`findReplyClaim` requires draft.userId, claim.userId === profile.userId) · idempotent twice · body text capped 60,000 chars; per-inbox rate limit; `intake.retryFailed` hourly | `convex/http.test.ts` (15: unsigned 401 no row; bad signature 401 empty; malformed JSON under valid signature 4xx; signed 2xx + one row; replay → still one row; oversized unsigned rejected; no-`thread` delivery accepted; GET falls through to static), `convex/inbound.test.ts` (13: dedupe, D23 steps 1–3, "never attaches a reply to another user's claim, even with a valid token", malformed payload, 6a-2 rate limit ×3, 6b-3 tombstone, 6b-7 owned rows). Audit repro `C05 webhook` PASS: signed reply to A's inbox carrying B's draft Message-ID **and** B's token routes to `intake`, never to B's claim; two deliveries of the same `event_id` under different svix ids → one row; tampered body under a valid signature → 401, empty body, no row. Live: T23 §2 check 5 `POST {} → 401` on `adorable-lion-138` (secret ABSENT by name there, so this exercises only the F-T22-1 branch). | VERIFIED_LOCAL @ba15aaf; live: the unconfigured-secret branch is VERIFIED_LIVE (dev), the signature path is **BLOCKED_EXTERNAL** (`AGENTMAIL_WEBHOOK_SECRET` not set on `adorable-lion-138`; no provider webhook pointed at it). | Agree. |
| C06 | `inbound` route=intake → `intake.processEvent` → `beginEvent` (tombstone → attempts → per-user `inbound_extract` → global `inbound_extract` → `processing`) → `lib/ai.extract("inbound_email", InboundEmail zod)` (untrusted text in `user` role, `MAX_INPUT_CHARS`) → `applyExtraction` (tombstone again; order → `needs_review` purchase (D25), refund → `promised_credit` only via `applyEvent` with `internalKey` (6a-1) + legacy key). Paste: `intake.paste` (tombstone-aware, 40–60,000 chars, per-user content hash) → `createPasteEvent` (charge `paste` only when new) → same pipeline. `retryEvent` (owner, `intake_retry` budget); `retryFailed` hourly (round-robin, tombstone-aware); `needsAttention` strips payload. | userId from row/ctx.auth at every hop · `processedEvents` status machine, `MAX_ATTEMPTS`, budget pause → `needs_review` (never counted as an attempt) · text 60k, paste 60k, `take(100)` on needsAttention | `convex/intake.test.ts` (62), `lifecycle.test.ts`, `budget.test.ts`; audit repro `spend gates` (same paste twice by one user → one event, one `paste` charge; B pasting the same text gets its own event) PASS. | VERIFIED_LOCAL @ba15aaf; live extraction **BLOCKED_EXTERNAL** (`OPENAI_API_KEY` placeholder on `adorable-lion-138`). | **Register says NOT_IMPLEMENTED — wrong.** Implemented and covered by 62 tests. |
| C07 | `purchases.create/confirm` (active, non-example) or `watches.markBought` → `schedulePolicyFetch` → `policies.fetchBoth` (`fetchBothImpl`: freshness check, global `policy_fetch`, Firecrawl search+scrape via component, OpenAI extraction, `lib/passage` verbatim-passage verification) → `insertSnapshot` (immutable rows; errors → confidence 0 note) → `policies.latest` ← `purchases.get`; `policies.refresh` (public action, `requireActiveUserId`, `beginRefresh` domain ownership, `policy_refresh` budget); `policies.confirm` (`ownedPolicy`; D116 caps on passage/sourceUrl/contactEmail). | owner on row; `confirm` owner-checked · snapshots append-only; per-domain freshness (`POLICY_REFETCH_MIN_AGE_MS`) · bounded by budgets and `POLICY_SCAN` reads | `convex/policies.test.ts` (31, incl. "refuses a tombstoned caller before charging or fetching anything"), `lib/passage.test.ts` (7), `lib/policyText.test.ts`. | VERIFIED_LOCAL @ba15aaf; **B2 open at ba15aaf**: `fetchBoth`/`insertSnapshot` have no `isTombstoned` gate (grep: `policies.ts` contains no `isTombstoned`) — a scheduled fetch for a just-deleted user still spends and writes (D124 condition → T18.5). Live: **BLOCKED_EXTERNAL** (`OPENAI_API_KEY` placeholder; `FIRECRAWL_API_KEY` is real on dev but the path needs both). | Agree on status; register omits the B2 condition. |
| C08 | cron `price watch` 2h → `priceWatch.runAll` → `eligibleItems` (per-user slice, rotation stamps, tombstone rest, `claims.by_item_type_status`) → `rotateAndSchedule` → `checkItem` → `itemForCheck` (tombstone; `parseProductUrl` re-check) → `observePrice` (Firecrawl scrape + OpenAI) → `recordCheck` (always inserts a `priceChecks` row; D16 gates in `rejectionReason`; window re-decided at write; `hasOpenPriceClaim` guard; settled-drop netting) → `openClaim` (`price_adjustment`, one open per item) → Board countdown. `checkNow` (owner, cooldown, `item_check` + global). | item.userId; `checkNow` owner-checked · always-insert observation; claim guard idempotent under concurrent checks · `PRICE_CHECK_PER_USER_PER_TICK`, `SCAN_LIMIT`, global `price_check` | `convex/priceWatch.test.ts` (51: D20 idempotency, D74 fairness, D87 tombstone skip, C3 resurrection with 500 transient fillers (6a-3), foreign item refused), `readBudget.test.ts` (eligibleItems 500×65 → 2,500 docs / 2,001 queries), `fairness.test.ts`. Audit repro `B4 (D124)`: **FAILS as designed** — `recordCheck` for a tombstoned owner opens a `claims` row (D124 B4, already conditioned → T18.5). Live: T23 §3.3 `priceWatch:runAll` → 0 on `adorable-lion-138` (scheduler entry runs; nothing due). | VERIFIED_LOCAL @ba15aaf with **B4 open**; scheduler entry VERIFIED_LIVE (dev, no-op tick); scrape/extract **BLOCKED_EXTERNAL** (`OPENAI_API_KEY` placeholder). | Register omits B4; otherwise agree. |
| C09 | `purchases.setReturned` (owner) → `claims.open` (owner; item must be `returned`; expected = `unitCents*qty − fee` derived server-side; returns policy attached if any) → `openClaim` (one `return_credit` per item unless dismissed) → ledger → `pages/Purchase.tsx`/`Claim.tsx`. | requireUserId + ownedItem · one open claim per item/type · n/a | `convex/claims.test.ts › open derives expected server-side and rejects unreturned item`, `› open with fee writes expected_change note`, `› D44 …`; `purchases.test.ts` (26). Live: T23 §4 `claims.spec.ts` 6 pass ×2 and `purchases.spec.ts` 4 pass ×2 on `adorable-lion-138`. | **VERIFIED_LIVE** @ba15aaf (dev e2e) / VERIFIED_LOCAL. | Agree (register VERIFIED_LOCAL + browser rehearsal; e2e upgrades it). |
| C10 | `claims.openClaim` re-checks `item.userId === userId`, `item.purchaseId === purchaseId`, `policy.userId`/`merchantDomain === purchase.merchantDomain`, `priceCheck.userId && itemId`; `purchases.confirm` requires every `items[].itemId` to belong to `purchaseId`; `drafts.approveAndSend` walks draft → claim → purchase by stored FKs; `inbound.findReplyClaim` requires draft.userId and claim.userId to equal the inbox owner. | yes · n/a · n/a | `claims.test.ts › D46: openClaim re-checks item, purchase and price-check relationships`, `› open rejects foreign policyId and mismatched priceCheck`; `purchases.test.ts` confirm cases; `boundary.test.ts`; audit repro C05 (cross-user Message-ID/token) PASS. | VERIFIED_LOCAL @ba15aaf. | **Register says NOT_IMPLEMENTED — wrong.** |
| C11 | `drafts.generate` (action; `requireActiveUserId`; `internal.drafts.context` then inline owner check; refuses confirmed/dismissed; `draft_generate` budget; OpenAI `DraftOut`; output truncated 80/1,200) / `drafts.insert` (internal; version = max+1 bound to `claim.version`; refuses past `MAX_DRAFTS_PER_CLAIM`) → `drafts.update` (owner; refuses once `outboundId`; `to` ≤320 (D116)) → `approveAndSend` (args ARE the approval, D58). | ownedClaim/ownedDraft · version mismatch throws · caps | `convex/drafts.test.ts` (49). Audit repro `C11/C12 approval` PASS: B refused; unconfirmed recipient refused with zero sends; stale claimVersion and stale draftVersion refused; `adjustExpected` bump makes the old approval unsendable; newest-draft rule; second click "already sent"; CRLF in `to` refused; example claim refused. | VERIFIED_LOCAL @ba15aaf; `generate` live **BLOCKED_EXTERNAL** (`OPENAI_API_KEY`). **B5 open at ba15aaf**: `drafts.insert` has no tombstone gate (D124 → T18.5). | Register says VERIFIED_LIVE (their prod) — `d88c5f8`, not the candidate (`drafts.ts` +188 lines since). **Status changed → VERIFIED_LOCAL.** |
| C12 | `approveAndSend` (one mutation: all checks → `charge(claim_email)` last → `agentmail.sendMessage` (component enqueue, transactional) → draft patched with `outboundId`/`approvedAt` → claim `queued` (status only; version untouched) → `scheduler.runAfter(30s, reconcileSend attempt 1)`) → `reconcileSend` (internal; tombstone skip; `agentmail.status`) → `applySendOutcome` (terminal failure → `drafted` + `sendError`, binding cleared; message id → consume F8 stash (N6: fast bounce → failed) → `sent` + thread + reminder; pending → backoff 30s/1m/2m/5m/10m; exhausted → `sendUnknown` and, only when `reschedule && status !== null`, one stall-hop re-arm (N2/N3)) → `recheckSend` (owner; `reschedule=false`) → `sendStatus` (owner) → Composer. `claims.dismiss` best-effort cancels an in-flight send (D57). | ownedDraft/ownedClaim · one enqueue per row (`outboundId` set in the enqueue txn); `MAX_SENDS_PER_CLAIM`; no system-table scan · bounded backoff + single stall chain | `convex/drafts.test.ts` (49: scheduler-driven backoff, N2 re-arm, N6 stash consumption, `MAX_SENDS_PER_CLAIM`, D57), `mailEvents.test.ts` (16). Audit repro PASS (claim goes `queued`, not `sent`, on approval). | VERIFIED_LOCAL @ba15aaf; live send/reconcile **BLOCKED_EXTERNAL** (`AGENTMAIL_API_KEY` placeholder, sends not authorized). | Agree. |
| C13 | `inbound.onMessageReceived` (D23: In-Reply-To/References vs `drafts.by_message` → `claims.by_thread` → `[RC-token]`; every hop `userId` re-checked) → `replies.classify` (per-user then global `inbound_extract` reservation; retry 20s/2m then `failEvent` with sanitized text) → `classifyOnce` (OpenAI `ReplyClass`) → `replies.apply` (dedupe `replies.by_message`; tombstone → writes nothing; `promised_credit` via `applyEvent` only with a stated positive amount, key = `internalKey(claim,"msg",messageId)` + legacy; `senderMismatch`; refusal/question → `attentionAt`; never `confirmed_credit`) → reminder → `claims.get` thread. | claim.userId === profile.userId at routing; `apply` internal · one reply per message id; ledger per-claim key · text 60k | `convex/replies.test.ts` (17), `inbound.test.ts` (D23 ×3, cross-user), `lib/idempotency.test.ts` (6). Audit repro C05 PASS. | VERIFIED_LOCAL @ba15aaf; live **BLOCKED_EXTERNAL** (OpenAI placeholder; no provider webhook). | Register says VERIFIED_LIVE (their prod) — `d88c5f8` (`replies.ts` +73, `inbound.ts` +58 since). **Status changed → VERIFIED_LOCAL.** |
| C14 | `pages/Claim.tsx` → `claims.confirmCredit` (owner; `idempotencyKey` ≤128 enforced here only, 6a-1) → `applyEvent` (dismissed refused; `assertPositiveCents`; `by_claim_key` dedupe: same facts → `deduped`, different facts → conflict; insert `ledgerEvents`; balance re-derived from the full ledger; `statusAfterEvent`; version+1; `cancelPending` on settle) → `lib/balance.claimBalance` → `purchases.board` totals (`netRecovered`, example rows excluded). | requireUserId + ownedClaim · per-claim key · ledger per claim read with `.collect()` (user-driven; measured 200 events = 203 docs) | `convex/claims.test.ts` (21: dedupe, D38 per-claim keys, 128-bound, D40, D48), `lib/ledger.test.ts`, `lib/balance`; audit repro `C14/C15 ledger` PASS (B refused "Claim not found"; 10.5 / −1 / 0 / 2^53+2 / NaN refused; replay `deduped:true`; conflict; same key on another claim independent; debit > net confirmed refused; `unresolved = 4000 − 1500 + 500 = 3000`; dismissed refuses). Live: T23 §4 `claims.spec.ts` + `resilience.spec.ts › confirming a credit completes with no pointer interaction` on `adorable-lion-138`. | **VERIFIED_LIVE** @ba15aaf (dev e2e) / VERIFIED_LOCAL. | Agree (e2e upgrades the browser rehearsal). |
| C15 | `claims.recordLaterDebit` (owner; key bound) → `applyEvent` (cap: `cents ≤ confirmed − debited`) → `later_debit` appended → `statusAfterEvent` → `reopened` when `unresolved > 0` → `Claim.tsx`. | ownedClaim · append-only; cap · n/a | `claims.test.ts › later debit 1000 reopens only this claim`, `› D40: a later debit cannot exceed net confirmed credit`; audit repro PASS (claim version 1→3 after two ledger events, status `reopened`). | VERIFIED_LOCAL @ba15aaf (+ browser rehearsal per register). | Agree. |
| C16 | `drafts.applySendOutcome` (sent) / `replies.apply` (promise) / `drafts.markPacketSent` → `followUps.scheduleClaimReminder` (`reminderFireAt` = max(7 d, returns window ≤365 d); cancel-then-schedule; schedule-first so the row always has `scheduledFnId`) → `followUps.fire` (only pending rows with `fireAt ≤ now`; confirmed/dismissed/tombstoned → cancelled; else `fired` + `attentionAt`) → `claims.attentionAt` → Board/Claim UI; `clearAttention` (owner). | claim.userId on row · status-gated; stale/duplicate runs are no-ops (D42) · `.collect()` on `followUps.by_claim` (one pending at a time; history grows one row per send/promise) | `convex/followUps.test.ts` (6), `drafts.test.ts` (reminder on sent; packet), `replies.test.ts`. | VERIFIED_LOCAL @ba15aaf. | Register says "pending opus checkpoint 2" — closed: VERIFIED_LOCAL. |
| C17 | `claims.adjustExpected` (owner; dismissed refused; version+1; unapproves any approved-but-unsent draft; `cancelPending`; note) / `claims.dismiss` (cancel reminders; version+1; cancel in-flight send) / `applyEvent` (version+1 on every ledger event) → `drafts.approveAndSend` requires `claim.version === args.claimVersion === draft.claimVersion` and `drafts.update` refuses on mismatch → `followUps` cancelled. | ownedClaim · version bump is the invalidation primitive · n/a | `claims.test.ts › adjustExpected bumps version, unapproves drafts, writes a note, no ledger event`, `› D41`, `› D57`; audit repro `C11/C12` (ledger/adjust bump → stale approval refused) PASS. | VERIFIED_LOCAL @ba15aaf. | **Register says NOT_IMPLEMENTED — wrong.** |
| C18 | `drafts.markPacketSent` (owner; refuses claims already `queued/sent/packet/confirmed/dismissed` (D52); note ≤500; claim → `packet`; reminder scheduled) → claim status → UI. | requireUserId + ownedClaim · status-gated · note cap | `drafts.test.ts › moves the claim to packet, writes a note and no ledger event, and reminds`, `› refuses a claim already queued, sent, packet, confirmed or dismissed (D52)`. | VERIFIED_LOCAL @ba15aaf. | **Register says NOT_IMPLEMENTED — wrong.** |
| C19 | `examples.load` (owner; idempotent when any `isExample` purchase exists; all contacts on `.example`; no network) → Board/Settings pill; `examples.remove` archive. Guards: `drafts.approveAndSend` refuses `claim.isExample || purchase.isExample`; `priceWatch.watchWindow` permanent-skips example purchases; `policies.beginRefresh` requires a non-example owner; `purchases.board` excludes example money. | requireUserId · idempotent · n/a | `convex/examples.test.ts` (7); audit repro (example claim cannot be mailed) PASS. Live: seeded example flows in T23 §4 purchases/claims specs. | VERIFIED_LOCAL @ba15aaf. Hygiene note: `purchases.create` accepts a client-supplied `isExample` flag (self-affecting only: excludes own totals, blocks the loader) — LOW, not registered. | Agree. |
| C20 | `App.tsx`: `/privacy` public; `/*` → `AuthGate` (`AuthLoading` splash, `Unauthenticated` → SignIn, `Authenticated` → `ConnectionBanner` + lazy routes, each wrapped in `RoutedPage` = keyed `ErrorBoundary` + `Suspense`) → pages → `useQuery` states (loading/empty/error/alert). | Authenticated gate; server functions re-check · n/a · route-level code splitting (largest chunk `vendor-react` 211 kB) | T23 §1 build; T23 §4 Playwright: auth 10, watches 6, purchases 4, claims 6, isolation 4, resilience 12 (all real, no `fixme` — `e2e/resilience.spec.ts` has only comments mentioning fixme), ×2 projects (desktop/mobile) on `adorable-lion-138` + local Vite; `src/lib/*.test.ts` (5 files). | **VERIFIED_LIVE** @ba15aaf (dev e2e; `src` identical to the run's HEAD). | Agree (e2e upgrades the browser rehearsals). |
| C21 | `convex.config.ts`: `agentmail` (env `AGENTMAIL_API_KEY` bound via the patch), `firecrawl`, `staticHosting`, `rateLimiter`; `http.ts` order: `auth.addHttpRoutes` → `POST /agentmail/webhook` → `GET/POST /alerts/unsubscribe` → `registerStaticRoutes` last. | n/a · n/a · n/a | `http.test.ts › GET /agentmail/webhook falls through to the static-hosting catch-all instead of 500ing`, `› auth discovery route`; T23 §1 codegen against the linked deployment (component bundling) clean. Live: OIDC 200 + webhook 401 on `adorable-lion-138` (T23 §2). | VERIFIED_LOCAL @ba15aaf; route registration VERIFIED_LIVE (dev) for the two non-static routes; static catch-all live not observable until a static deploy. | **Register says NOT_IMPLEMENTED — wrong** (implemented; tested; partly live). |
| C22 | `npx convex codegen` (needs a linked deployment) → `convex/_generated/{api,dataModel,server}.d.ts` committed → `tsc -p tsconfig.app.json && tsc -p convex/tsconfig.json`. | n/a · `scripts/check-codegen.mjs` drift check · n/a | T23 §1: codegen against `dev:adorable-lion-138` → `git diff --stat` empty at 529f545 (convex identical to ba15aaf); typecheck PASS locally (T23) and in CI 35625965752. `codegen:check` in CI SKIPPED (no `CODEGEN_CHECK_CONVEX_URL`/`_ADMIN_KEY` secrets). | VERIFIED_LOCAL @ba15aaf; CI drift gate **BLOCKED_EXTERNAL** (repo secrets `CODEGEN_CHECK_CONVEX_URL`, `CODEGEN_CHECK_CONVEX_ADMIN_KEY` not provisioned). | **Register says NOT_IMPLEMENTED — wrong** (generated API committed and verified drift-free). |
| C23 | Server-only secrets read fresh from `process.env` at call time (`lib/ai.ts`, `profiles.ts`, `account.ts:inboxTransport`, `market.ts`, `lib/authMail.ts`, `http.ts` name-only); client bundle carries only `VITE_CONVEX_URL`; `lib/log.logEvent` redacts `Bearer …`, `sk-`, `fc-`, `whsec_` and truncates; `lib/errors.sanitizeError` buckets user-visible errors; provider response bodies never echoed (`createInboxRemote`, `deleteInbox`). | n/a · redacted errors · n/a | `convex/lib/log.test.ts` (18), `lib/errors.test.ts` (4), `boundary.test.ts` secret-substring leak checks, `http.test.ts` (empty 401 bodies), `account.test.ts` (`deleteInbox` bearer header by name, body never echoed — 6b-8). Live: T23 §3.5 `checkWatch` log line `{"kind":"price_check_failed",…,"error":"Provider error"}` with no key/stack. Remaining bare `console.error`: only `lib/authMigrate.ts:116,137` (ids only). | VERIFIED_LOCAL @ba15aaf; redaction VERIFIED_LIVE (dev, one line). | **Register says NOT_IMPLEMENTED — wrong.** |
| C24 | `npm run deploy` (`@convex-dev/static-hosting deploy`) → `<deployment>.convex.site` serves `dist/` behind the catch-all; `SITE_URL`/`CONVEX_SITE_URL` feed auth redirects and the unsubscribe base (`unsubscribeBase()` from `CONVEX_SITE_URL`, D92a); webhook URL configured at the provider. | n/a · `scripts/smoke.mjs` (7 checks) · n/a | T23 §2 on `adorable-lion-138`: checks 0–4 FAIL (503/404 — no static build deployed), 5–6 PASS. No lane has deployed the candidate's `dist/` anywhere; `cool-oyster-399` still serves `d88c5f8` (I7a, 2026-09-20). | **BLOCKED_EXTERNAL** @ba15aaf: production deploy not authorized (D83 item 6); no static deploy of the candidate to the dev host has been performed either (an internal action the lead could take, not an external block — but until it happens the public-site half of this connection is unverified for the candidate). | Register says VERIFIED_LIVE (their prod) — that is `d88c5f8`. **Status changed → BLOCKED_EXTERNAL (candidate not deployed).** |
| C25 | `watches.create` (owner; `parseProductUrl`; name cleaned ≤200; `consumeCreateLimit` 20/h & 50 live; global `price_check`; first check scheduled in the same txn; `nextCheckAt` bumped) → `checkWatch` → `watchForCheck` (archived/bought/tombstoned → null) → `observePrice` → `recordWatchCheck` (always a `watchChecks` row; D16 rejection; `lastObservedAt` only on accepted reads; product name/image only when `variantMatch !== "none"`; `claimDrop`; `market.requestLookup` auto) → `watches.list/get` (coarse `now`, `assertCoarseNow`; tombstone → empty) → WatchCard; cron `sweep` hourly (`by_status_nextCheck`, `WATCH_SWEEP_PAGE` 50, per-user 10, tombstone rest, budget-cut rows left due, every scanned row bumped). | ownedWatch; sweep uses row owner · idempotent (bump in the scheduling txn) · budgets fail closed | `convex/watches.test.ts` (63), `freshness.test.ts` (5), `fairness.test.ts` (4; 1 accepted `it.fails` F-D74-1), `readBudget.test.ts` (`watches.list` 1,261 docs; `sweep` 100 docs), `lifecycle.test.ts › watches.sweep skips a tombstoned owner's due watch`. Live: T23 §3.3 `watches:sweep` → 0; §3.5 `watches:checkWatch` on a fake domain through the REAL Firecrawl key → caught, note-only row, one redacted log line ("fail-closed invariant holds end-to-end"); §4 `watches.spec.ts` 6 pass ×2. | **VERIFIED_LIVE** @ba15aaf (dev: sweep tick, failed-read path with the real Firecrawl key, e2e); successful extraction still **BLOCKED_EXTERNAL** (`OPENAI_API_KEY` placeholder). | Register says VERIFIED_LOCAL (F2 → T12.1): F2 closed (D107/D112); e2e + §3.5 upgrade the row. |
| C26 | `recordWatchCheck` → `notify.claimDrop` (active watch; `isAlertableDrop`; dedupe `watch:<id>:<cents>`; re-claim only transient reasons after 24 h AND re-passing `alertGate` + daily cap + global `drop_email` (F6); one txn) → `sendDrop` (internal; only `claimed` rows; `alertGate` again; watch active; `ALERTS_INBOX_ID` or profile inbox; fixed-shape body; `List-Unsubscribe` from `CONVEX_SITE_URL`; `sendMessage` in try → `failed/send_failed` sanitized; `queuedPatch` + `reconcileDrop` outside the try (F11b)) → `applyDropOutcome` (`sent` only with a message id (or provider `complained`); terminal → `failed` + suppression; backoff; exhausted → `unknown` + stall re-check) → `sweepStalled` hourly (claimed/queued/unknown past `nextCheckAt`) → `recheckDrop` (owner, 1/min) → `notify.drops` → Watching chips. `mailEvents.onEvent` (bounce/complaint on `sent` rows → failed + `suppressAddress`; F8 stash otherwise). | userId on row; tombstone at every suppression site · one enqueue per row (`status !== "claimed"` refuses; OCC on the same doc) · `DROP_EMAIL_COUNT_SCAN`, `MAX_DROP_EMAILS_PER_DAY` 5 | `convex/notify.test.ts` (55), `notify.fault.test.ts` (14: "two concurrent sendDrop calls … enqueue exactly once", crash windows), `mailEvents.test.ts` (16), `alerts.flow.test.ts` (7), `lifecycle.test.ts › notify.claimDrop for a tombstoned owner's watch never enqueues or sends`; audit repro `C26 alert gate` PASS (gate order deleted > no_email > unverified > opted_out > address_suppressed; `sendDrop` re-suppresses a row claimed before deletion, zero sends). Live: T23 §3.3 `notify:sweepStalled` → 0; §3.5 `reconcileDrop` on the E2E fixture's synthetic `outboundId` throws an uncaught `ArgumentValidationError` (F-T23-1b, fixture scope). Historical: hackathon.md 2026-09-20 "a drop email went out once through AgentMail" — pre-mission revision, deployment unnamed for W2. | VERIFIED_LOCAL @ba15aaf; live send **BLOCKED_EXTERNAL** (`AGENTMAIL_API_KEY` placeholder, `ALERTS_INBOX_ID` absent, real sends not authorized). | Agree. |
| C27 | `watches.markBought` (owner; refuses bought/archived; per-user purchase cap; `orderRef` cleaned ≤100; creates purchase + item with `productUrl`; sets `watch.purchaseId` once; `status: "bought"`; `schedulePolicyFetch`) → `watchForCheck`/`requestLookup` refuse `bought`; `insights.activity/sources` and `tracking.overview` count a bought watch nowhere (D72). | ownedWatch · second call refused · n/a | `watches.test.ts` (markBought cases), `insights.test.ts` (32, DA-18a), `dashboard.test.ts` (5). Live: T23 §4 `watches.spec.ts`/`purchases.spec.ts` (bought watch fixture, tracked item table). | VERIFIED_LOCAL @ba15aaf (+ e2e coverage of the bought fixture). | Agree. |
| C28 | `offers.find` (owner; active/paused; named or priced; `consumeFindLimit` cooldown 6 h + 10/day; marker row; schedule) → `search` → `watchForSearch` (tombstone before spend, F12) → `searchOffers` (Firecrawl search ≤8, ≤5 scrapes/extractions) → `recordCandidates` (never `best`; out-of-stock unpriced; per-watch cap 40; tombstone no-op) → `confirm/reject` (`ownedOffer`; `productName` set once) → `dueForRecheck`/`recheck` (cron 13:00 UTC + ride-along on accepted checks) → `recordRechecks` (drift → `NEEDS_RECONFIRM_NOTE`, same-currency ranking) → `listForWatch` (coarse `now`; tombstone → EMPTY). | ownedWatch/ownedOffer · marker-row counting; caps · bounded pages | `convex/offers.test.ts` (35), `lib/offerMatch.test.ts`, `lib/shopsavvy.test.ts`; `readBudget.test.ts` (`listForWatch` 101 docs). | VERIFIED_LOCAL @ba15aaf; live **BLOCKED_EXTERNAL** (OpenAI placeholder; not run live for the candidate). | Register says "F4 HIGH open → T13.1": T13.1 accepted (D105); F4 closed. |
| C29 | `market.refresh` (owner) / auto from `recordWatchCheck` → `requestLookup` (one txn: archived/bought/tombstone → refuse; no key → `not_configured` with no stamp; stale `queued/running` reclaim after 15 min; `success` fresh 7 d; `terminal_failure` manual resets attempts; charge per-user for new/manual, global-only for retries (C5)) → `lookup` (Content-Length and 2 MB cap before buffering) → `recordSnapshot` (state must still be `running`; currency filter F5a) → `migrateStamps`. | ownedWatch on refresh; row owner on auto · one paid call under race; attempts reset on success/empty · caps | `convex/market.test.ts` (34: "lets exactly one of two concurrent requestLookup calls claim and charge", Content-Length cap, DA-7, F9), `marketFlow.test.ts`; audit repro (B's watch refused) PASS. Live: T23 §3.3 `market:migrateStamps` → `{scanned:10, migrated:0, done:true}`; §5 real lookup BLOCKED. | VERIFIED_LOCAL @ba15aaf; migration entry VERIFIED_LIVE (dev); lookup **BLOCKED_EXTERNAL** (`SHOPSAVVY_API_KEY` absent by name on `adorable-lion-138`). | Agree. |
| C30 | `lib/verdict.verdictWithQualifier` (pure), `lib/freshness.isPriceStale` (3 d), `lib/shopsavvy` filters (median band, variant/bundle rejection) → `watches.get`/`insights.trackedTable` (`priceStale` + `qualifiedReason`) → WatchCard / `StaleBadge`. Provider data reaches `offers` candidates and `marketPrices` only; `claimDrop`/`openClaim` are called solely from `recordWatchCheck`/`recordCheck` with Recoup's own observation (call-site trace re-done at ba15aaf: `claimDrop` has one caller, `openClaim` two). | n/a · n/a · n/a | `lib/verdict.test.ts` (40), `lib/shopsavvy.test.ts` (29), `freshness.test.ts` (5, F-T15-1 flipped), DA-19a in `offers.test.ts`. | VERIFIED_LOCAL @ba15aaf. | Register says "F10 legacy-stale fix → T12.1": closed (`priceObservedAt`/`priceStale`, D118/D120). |
| C31 | Verification/reset codes: `Password({ verify: authMail("verify"), reset: authMail("reset") })` (8-digit codes generated by the library, provider ids `recoup-verify`/`recoup-reset`, 15 min) → `lib/authMail.authMailTransport.send` (AgentMail REST from `ALERTS_INBOX_ID`; per-address 3/h and global 200/h; E2E capture hook refuses on the production host) → SignIn steps. Preferences: `alerts.settings/setAlerts` (owner; re-enable clears suppression and rotates the token) → `alertGate` at claim and send time → `unsubscribeByToken` via `GET` (page only, writes nothing) / `POST /alerts/unsubscribe` (token ≤128 chars, always 200, never reveals validity). | requireUserId (tombstone-aware) · phantom-consume for unknown resets; `authSignUpGlobal` 200/h · bounded | `authFlow.test.ts`, `auth.test.ts`, `lib/authMail.test.ts`, `alerts.test.ts` (13), `http.test.ts › GET/POST /alerts/unsubscribe` (4); audit repro `C31 unsubscribe` PASS (1-char-off token no-op; GET no-op; POST disables; rotated token after re-enable no-op; 100 KB token → 200). Live: T23 §4 `auth.spec.ts` (codes via the E2E hook). | VERIFIED_LOCAL @ba15aaf (auth loop VERIFIED_LIVE on dev with the E2E transport, see C02); real mail **BLOCKED_EXTERNAL**. LOW (D124): reset-code mail is still sent for a tombstoned user (no session results). | Register says "N1/N4/N5 → T05.2": landed and verified (D107). |
| C32 | Settings → `account.exportPage` (owner; 19-table literal union; direct tables via `by_user` paginate 200; `processedEvents` per-status pages of 25 (6b-6); via-parent tables through a client-supplied JSON cursor whose every parent id is re-verified with `isOwnedParent` and every child filtered by owner (6b-1); `pid`/`queue` disjoint so a parent with >200 children terminates (6b-2)) / `account.requestDeletion` (`getAuthUserId` by design; exact phrase; idempotent; captures `inboxId` on the tombstone; revokes sessions + refresh tokens; suppresses queued mail; schedules `purge` and records `activePurgeJobId`) → `purge` (≤500 `purgeStep`s per run then reschedule; `inboxTransport.deleteInbox` (404 = success, body never echoed) with 1m/10m/1h/6h/24h retries; `mailPurge.purgeInboxData` (component patch: `purgeInbox` per inbox, ≤200 iterations, truthful `complete`); `purgeAuth` runs on success OR exhaustion (6b-4b); `finishPurge` `deleted` with literal `inboxDeleted`) → cron `reDriveStuckDeletions` (24 h; `db.system.get` on `activePurgeJobId`, no scan; ≤`STUCK_DELETION_REDRIVE_PAGE`) → `deletionStatus` (readable while tombstoned) → Settings panel (page cap on export; literal `inboxDeleted`/`mailDataPurged`/`attempts`). `auth.beforeSessionCreation` blocks new sign-ins. | requireUserId / ctx.auth only · idempotent tombstone; resumable purge; bounded retries · byte-aware pages | `convex/account.test.ts` (45: 6b-1 forged `queue`/`pid`/nonexistent, 6b-2 termination, 6b-6, 6b-8 `deleteInbox` stub matrix, "another user's requestDeletion never tombstones the caller", stuck re-drive), `lifecycle.test.ts` (13, all real), `mailPurge.test.ts` (3), `auth.test.ts` (tombstoned sign-in); audit repro `C32` PASS (forged cursor with B's id in `queue`×2 + `pid` + `skip: 1e6` → `[]`, cursor `null`; garbage cursor → `[]`; wrong phrase refused; idempotent; B untouched; every write/read for tombstoned A refused or empty; `deletionStatus` readable). Live: T23 §3.3 `account:reDriveStuckDeletions` → `{rescheduled: 0}`. | VERIFIED_LOCAL @ba15aaf with the D124 conditions open at this hash (B1 component `outboundMessages`/`events` for shared-inbox alerts survive purge; B2–B5 tombstone gates; B6 ops/RUNBOOK) — all assigned to T18.5; remote inbox deletion and component purge live **BLOCKED_EXTERNAL** (`AGENTMAIL_API_KEY` placeholder). | **Register says NOT_IMPLEMENTED (T18/T19/T21) — stale.** T18/T18.1–T18.4, T19, T21 landed (D116–D124); status → VERIFIED_LOCAL with T18.5 conditions. |
| C33 | `.nvmrc` 22 → `npm ci` (scripts on; `postinstall: patch-package --error-on-fail`) → `scripts/check-patch.mjs` → typecheck → `oxlint` → `scripts/check-test-count.mjs 600` → build → `scripts/check-codegen.mjs` (skips without secrets) → `e2e` job (skips without `E2E_CONVEX_URL`/`E2E_DEPLOY_KEY`); `.github/workflows/ci.yml` least-privilege, cancel-in-progress. | n/a · patch asserted twice · n/a | T23 §1 clean-worktree gates 8/8 at 529f545; my worktree at ba15aaf: `npm ci` 0 vulnerabilities, `verify:patch` OK, vitest 1287+1; **GitHub Actions run 35625965752 on ba15aaf: `checks` ✓ 2m52s ("1288 tests passed across 65 file(s)"), `e2e` ✓ 44s (Playwright step skipped: secrets absent)**. `codegen:check` prints "SKIPPED". | **VERIFIED_LIVE** @ba15aaf for the CI pipeline itself (observed green on GitHub); `codegen:check` and the CI Playwright run remain **BLOCKED_EXTERNAL** (repo secrets `CODEGEN_CHECK_CONVEX_URL`, `CODEGEN_CHECK_CONVEX_ADMIN_KEY`, `E2E_CONVEX_URL`, `E2E_DEPLOY_KEY` not provisioned). Local-Node caveat F-T23-1 (v25 locally; CI pins 22). | Register says "CI on GitHub not yet observed running (BLOCKED_EXTERNAL until a PR/push triggers it)": **status changed → VERIFIED_LIVE (CI observed) with two secret-gated steps still BLOCKED_EXTERNAL.** |
| C34 | `budget.status` (owner; coarse `now`) → `BudgetBanner`; `lib/errors.sanitizeError` + `processedEvents.errorSummary` (D58) → Board attention list; `lib/log.logEvent` (correlation id, redaction) → `npx convex logs`; `ops.pauseKind/resumeKind` (internal; writes the global `usage` row that `tryConsumeGlobalBudget` reads, so retries and re-claims pause too — F6) and `ops.backlog` (bounded counts, `truncated`, retention cursor age) → RUNBOOK §1/§4/§5. | internal-only ops; per-user status · global switches fail closed · `scanLimit` | `convex/ops.test.ts` (24), `budget.test.ts` (18), `lib/log.test.ts` (18), `notify.test.ts` (F6 kill-switch pauses re-claims). Live: T23 §3.1/3.4 `ops:backlog` before/after on `adorable-lion-138` (truthful +1 delta for the seeded row; retention cursor age moved). | **VERIFIED_LIVE** @ba15aaf (dev: `ops:backlog`) / VERIFIED_LOCAL. **B6 open at ba15aaf**: `account.stuckDeletions` is not wired into `ops.backlog` and RUNBOOK has no "Account deletion" section (D124 → T18.5). **F-T23-3 open**: RUNBOOK §11 documents a non-existent `--inline-mutation` flag (`grep -c inline-mutation docs/ops/RUNBOOK.md` = 1 at ba15aaf). | Register says PARTIAL (correlation ids/ops switches NOT_IMPLEMENTED → T22): T22 landed (D109/D118). **Status changed → VERIFIED_LIVE (dev) with B6 + F-T23-3 open.** |
| C35 | `lib/authMigrate.normalizeLegacyAccounts` (resumable pages, collisions logged and left, `restart` arg N8) and `market.migrateStamps` (state classification, no charge) → RUNBOOK §3; additive-only schema (§8); backup/restore = `npx convex export`/`import` (§9, commands verified against the CLI help, never run); retention sweep (daily, resumable). | n/a · idempotent/resumable · pages | `lib/authMigrate.test.ts` (6), `market.test.ts` (migrateStamps), `retention.test.ts` (13). Live: T23 §3.3 `market:migrateStamps` and `retention:sweep` ran clean on `adorable-lion-138`. Backup/restore proof: **NOT performed** (T25). Rollback proof: none. | VERIFIED_LOCAL @ba15aaf for migrations (both also VERIFIED_LIVE as dev no-op runs); backup/restore/rollback proof **NOT_IMPLEMENTED** (T25 not started; release manifest absent). | Agree (PARTIAL) — restated with the vocabulary: migrations VERIFIED, backup/restore NOT_IMPLEMENTED. |
| C36 | `RoutedPage` (`ErrorBoundary` keyed by pathname; focus to heading; role=alert), `ConfigMissing`, `ConnectionBanner` (aria-live), lazy routes → `e2e/resilience.spec.ts` (12: malformed/foreign ids → fallback/not-found with focus + way back; keyboard-only sign-in and credit confirmation; axe on 6 pages ×2 projects). | Authenticated gate · n/a · n/a | T20 (D113) + T23 §4: resilience 12 pass ×2 (3 flaky-then-pass at `--workers=2`, clean at the suite's `workers: 1`); axe fixmes all flipped (D120/D123). Offline/reconnect banner: T03 manual rehearsal only, no e2e (NONE automated). | **VERIFIED_LIVE** @ba15aaf (dev e2e) for routes/errors/a11y/keyboard; reconnect banner VERIFIED_LOCAL (manual rehearsal only). | Register says "VERIFIED_LOCAL (T03); e2e in progress": e2e landed. **Status changed → VERIFIED_LIVE (dev).** |

---

## 2. The 17 product invariants (one line each: enforcing code → proving test)

| # | Invariant | Enforcing code | Test |
|---|---|---|---|
| 1 | Integer cents everywhere | `lib/money.ts:assertCents/assertPositiveCents/assertQty` (safe-integer, sign); `toCents` rounds half-away; `rejectionReason` refuses non-safe-integer/≤0 observed cents | `lib/money.test.ts`; audit repro `C14/C15 ledger` (10.5 / −1 / 0 / 2^53+2 / NaN refused) |
| 2 | Only user confirmation creates `confirmed_credit` | grep at ba15aaf: the only production writer of `"confirmed_credit"` is `claims.confirmCredit` (public, `requireUserId` + `ownedClaim`); `replies.apply` and `intake.applyRefund` pass the literal `"promised_credit"`; `applyEventInternal` accepts any kind but is `internalMutation` with no production caller | `replies.test.ts` (promise → promised only), `intake.test.ts` (refund → promised), `hackathon.md` Inv 3 |
| 3 | `unresolved = expected − confirmed + later debits` (promise never changes it) | `lib/ledger.balance` (promised = latest, confirmed/debited = sums, unresolved unclamped) | `lib/ledger.test.ts`; `claims.test.ts › promise 4000 … leaves unresolved 4000`; audit repro (4000−1500+500 = 3000) |
| 4 | Append-only ledger | single `ctx.db.insert("ledgerEvents", …)` at `claims.ts:256`; no `patch`/`delete` of `ledgerEvents` anywhere outside `account.purgeStep` (account deletion) and `retention.ts` never prunes it | `claims.test.ts`; grep evidence above |
| 5 | Per-claim idempotency | `ledgerEvents.by_claim_key` lookup in `findLedgerDuplicate` (+ legacy key, 6a-1); same facts → `deduped`, different → conflict; empty key refused; 128-char bound on client keys only, `internalKey` hash for internal callers | `claims.test.ts › D38`, `› duplicate idempotency key writes one event`, `lib/idempotency.test.ts`; audit repro |
| 6 | Approval bound to draft/claim version | `drafts.approveAndSend`: `draft.version === args.draftVersion`, newest-draft check, `claim.version === args.claimVersion === draft.claimVersion`; `applyEvent`/`adjustExpected`/`dismiss` bump `claim.version`; `drafts.update` refuses on mismatch | `drafts.test.ts` (version cases), `claims.test.ts › adjustExpected …`; audit repro `C11/C12` |
| 7 | Queued until provider message id | `approveAndSend` sets `queued`; `applySendOutcome` sets `sent` only in the `status.agentmailMessageId` branch; `notify.applyDropOutcome` sets `sent` only with a message id (or provider `complained`); `outboundId` written in the enqueue transaction, second click refused | `drafts.test.ts` (reconcile/backoff/N2/N6), `notify.test.ts`, `notify.fault.test.ts`; audit repro (claim `queued` after approval) |
| 8 | Recipient gate | `approveAndSend`: unticked recipient must equal `confirmedContactFor` (a `confirmedByUser` policy snapshot's contact); `stripControl` + `EMAIL_RE`; example claims/purchases refused | `drafts.test.ts` (D18 cases); audit repro ("Confirm this recipient" with zero sends; CRLF refused; example refused) |
| 9 | Webhook verify + dedupe | `http.ts` (secret by name → 401; component svix verify → 401 empty; throw → 401) → component `events.by_eventId` → `processedEvents.by_external` | `http.test.ts` (15), `inbound.test.ts › dedupes a redelivered event id`; audit repro `C05` |
| 10 | No wall clock in reactive queries | awk scan of every `export const X = query(` body at ba15aaf: zero `Date.now()`; display queries take coarse `now` through `assertCoarseNow` (which reads the clock only to throw on a bad argument) | `freshness.test.ts`, `watches.test.ts` (coarse-now bounds), `tracking.test.ts`, `insights.test.ts` |
| 11 | Bounded reads | per-user slices/`take`/paginate in sweeps, insights, tracking, export/purge (byte-aware for `processedEvents`); measured in `readBudget.test.ts`/`read-budgets.md` (all < 4,096 ranges, < 32,000 docs). **Exception found: `purchases.board` (Board page) — see F-AUD-1.** | `readBudget.test.ts`, `tracking.test.ts › 60×50×12 … < 4,096`, `insights.test.ts`; audit repro `zz_audit_board.test.ts` **FAILS at 100×50** |
| 12 | Tombstone gates | `requireUserId` (41/57 public fns), `isTombstoned` in every `getAuthUserId`-branch query, every sweep, `sendDrop`/`claimDrop`/`alertGate`, `inbound`, `intake.beginEvent/applyExtraction/createPasteEvent`, `replies.apply`, `reconcileSend`, `followUps.fire`, `market.requestLookup/recordSnapshot`, `offers.*`, `auth.beforeSessionCreation`. **Open at ba15aaf (D124): `policies.fetchBoth/insertSnapshot`, `profiles.save`, `priceWatch.recordCheck`, `drafts.insert`.** | `lifecycle.test.ts` (13), `account.test.ts`, per-module 6b-3 tests; audit repro `B4` **FAILS as designed** (claim opened for a tombstoned owner via `recordCheck`) |
| 13 | Alert gate order | `lib/accountState.alertGate`: deleted → no_email → unverified → opted_out → address_suppressed; evaluated at `claimDrop` (fresh and re-claim) and again in `sendDrop` | `alerts.flow.test.ts` (7), `notify.test.ts`; audit repro `C26 alert gate` PASS |
| 14 | One open claim per item (cron vs check-now) | `openClaim` scans `claims.by_item` and throws on an open same-type claim; `recordCheck` asks `hasOpenPriceClaim` first (no-op, no throw) | `priceWatch.test.ts › opens at most one claim across repeated qualifying checks (D20 idempotency)`, `claims.test.ts › D44` |
| 15 | Failed/rejected reads are stored, never shown as current | `recordWatchCheck`/`recordCheck` always insert a row; `observedCents` undefined on rejection; `lastObservedAt` only on accepted reads; `priceStale` 3 d → verdict unknown | `watches.test.ts`, `freshness.test.ts`; T23 §3.5 live failed read → note-only row |
| 16 | Provider data never creates a claim or alert | call-site trace at ba15aaf: `claimDrop` ← `recordWatchCheck` only; `openClaim` ← `claims.open` and `priceWatch.recordCheck` only; ShopSavvy writes `marketPrices`/offer candidates only | `lib/shopsavvy.test.ts`, `offers.test.ts` DA-19a, `market.test.ts` |
| 17 | Budgets fail closed | `lib/budget.tryConsumeGlobalBudget`/`charge` refuse at max; `overDailyCap` counts anything unclear as over; `pauseKind` pins the same row; charged after every ownership/status refusal and before the paid work | `budget.test.ts` (18), `ops.test.ts`, `notify.test.ts` F6, endpoint-inventory "charges after refusals" re-verified per function |

---

## 3. Findings register (capped at 12; severity · repro · file:line · suggested owner)

| ID | Sev | Finding | Repro | Where | Owner |
|---|---|---|---|---|---|
| F-AUD-1 | **HIGH** (availability of the main page for heavy accounts; same class as D107 C1/C2) | `purchases.board` — the Board page's only query — does `1 + purchases + items×(claims + ledger)` index ranges: at 100 purchases × 50 items (half `MAX_PURCHASES_PER_USER`=200, at `MAX_ITEMS_PER_PURCHASE`=50) it throws `Too many index ranges read in a single function execution (limit: 4096)`; the Board renders nothing but the error boundary for that user. 60×50 passes. Two further unbounded `.collect()`s in the same query (`processedEvents` `failed` and `needs_review` per user, never pruned by retention — only their payloads are) grow with a flooded inbox. | `scratchpad/audit/repros/zz_audit_board.test.ts` (`transactionLimits: true`, 100×50, no claims): FAIL with the quoted platform error | `convex/purchases.ts:354-432` (`board`); `src/pages/Board.tsx:58` | sonnet-backend (purchases.ts): cap purchases per board page (`take`) and page items/claims like `tracking.overview` (D107 C1), `take(20)` on each `processedEvents` status; add a 200×50 regression under `transactionLimits: true`; measure in `read-budgets.md` |
| F-AUD-2 | MEDIUM (provider resource spend by any signed-in user; residue survives deletion) | `profiles.ensureInbox` is a public action whose check→create→save is not atomic and is not rate-limited: N concurrent calls by one user issue N `POST /inboxes`; `save` keeps the first, the other N−1 provider inboxes are live, unowned, never routed to and never deleted by `account.purge` (only `profile.inboxId` is). `rateLimits.ts:inboxProvision` (1 per 5 min) is defined and never called — the endpoint inventory's "idempotency already prevents a live double-provision" is wrong for the concurrent case. | `zz_audit.test.ts › C04 ensureInbox`: 5 concurrent calls → 5 POSTs, 1 profile row | `convex/profiles.ts:158-171`; `convex/lib/rateLimits.ts:40` | sonnet-backend (profiles.ts): take `inboxProvision` in a mutation before the fetch (e.g. inside `requireActiveUserId` made a mutation, or a `claimProvision` internalMutation); on a lost `save` race, DELETE the just-created inbox (same fix shape D124 B3 already prescribes for the tombstone race) |
| F-AUD-3 | MEDIUM (truthfulness of the register) | Four rows (C04, C11, C13, C24) are VERIFIED_LIVE on the strength of the collaborator's I7a run of `d88c5f8` on `cool-oyster-399` (2026-09-20). The candidate is 1,242 insertions ahead in exactly those files and has never been deployed with real provider keys; the only live runs of the candidate are on `adorable-lion-138` with placeholder OpenAI/AgentMail keys. | `git diff --stat d88c5f8 ba15aaf -- convex/{auth,drafts,http,inbound,notify,profiles,replies}.ts` | `docs/team/CONNECTIONS.md` rows C04/C11/C13/C24; `docs/team/VERIFICATION.md:45` | lead: relabel as "VERIFIED_LIVE @d88c5f8 (historical); candidate VERIFIED_LOCAL / BLOCKED_EXTERNAL" |
| F-AUD-4 | MEDIUM (D124 B1–B6, re-confirmed open at ba15aaf) | Tombstone gates missing on `policies.fetchBoth`/`insertSnapshot` (B2), `profiles.save` (B3), `priceWatch.recordCheck` (B4 — repro opens a claim mid-purge), `drafts.insert` (B5); shared-inbox alert `outboundMessages`/`events` survive purge (B1); `stuckDeletions` not in `ops.backlog`, no RUNBOOK "Account deletion" section (B6). | `zz_audit.test.ts › B4 (D124)` FAILS as designed; grep `isTombstoned` in `policies.ts` → none; `ops.ts` → no `stuckDeletions` | as listed | T18.5 (sonnet-backend) — in flight in the shared checkout at audit time (uncommitted edits to `account.ts`, `mailPurge.ts`, the patch); not on `origin/main` |
| F-AUD-5 | MEDIUM (operator doc defect, F-T23-3 re-confirmed) | `docs/ops/RUNBOOK.md` §11 tells the operator to reset a stalled retention cursor with `npx convex run --inline-mutation`, which does not exist in `convex@1.46.0`. | `grep -c inline-mutation docs/ops/RUNBOOK.md` = 1 | `docs/ops/RUNBOOK.md` §11 | T18.5/T25 (RUNBOOK owner): add `ops.resetRetentionCursor` internal mutation or document the code-change path |
| F-AUD-6 | MEDIUM (release completeness) | No release manifest, no backup/restore proof, no rollback rehearsal (T25 not started); the candidate's `dist/` has not been deployed to any host, so `smoke.mjs` checks 0–4 (deep links, single bundle host) have never passed for the candidate. | T23 §2 (5/7 FAIL), `docs/team/PLAN.md` T25 row | `docs/ops/RUNBOOK.md` §9; `scripts/smoke.mjs` | lead/T25: static-deploy the candidate to `adorable-lion-138` (internal action, no external block), run `npm run smoke`, run `npx convex export` once against dev and `import` into a scratch deployment, write the manifest |
| F-AUD-7 | LOW (test-fixture scope, F-T23-1b) | `testing.seedFixtures`' queued `mailLog` row carries a synthetic `outboundId`; `notify.reconcileDrop`/`recheckDrop` on it throw an uncaught `ArgumentValidationError` from the component's `status()` validator. Unreachable in production (real ids come from `sendMessage`), but any future live smoke that reconciles a seeded row will misread this as a product crash. | T23 §3.5 item 1 | `convex/testing.ts` (`seedFixtures`), `convex/notify.ts:576` | sonnet-tester: document the row as render-only or drop it |
| F-AUD-8 | LOW (hygiene) | `convex/boundary.test.ts` and `convex/notify.test.ts` contain non-UTF-8 bytes (`file` reports "data"; `grep` without `-a` prints nothing for them) — the D124 LOW named only `notify.test.ts`. | `file convex/boundary.test.ts convex/notify.test.ts` | those two files | sonnet-tester |
| F-AUD-9 | LOW (self-affecting) | `purchases.create` accepts a client-supplied `isExample: true`, which excludes the caller's own purchase from money totals/spend and blocks `examples.load` for them. No cross-user effect. | code read | `convex/purchases.ts:94,143` | sonnet-backend: drop the arg from the public validator (the loader inserts directly) |
| F-AUD-10 | LOW (hygiene) | `lib/authMigrate.ts:116,137` still use bare `console.error` (ids only, no secrets) instead of `logEvent("migration_progress")`; RUNBOOK §5 says all sites were swept. | grep | `convex/lib/authMigrate.ts` | T24 backend pass |
| F-AUD-11 | LOW (test invocation, F-T23-2 re-confirmed) | Playwright suite races its shared per-project lead account at `--workers>1`; `playwright.config.ts` says so but nothing enforces it and CI's e2e job (when secrets exist) does not pin `--workers=1`. | T23 §4.1 | `playwright.config.ts`, `e2e/fixtures.ts`, `.github/workflows/ci.yml` | sonnet-tester |
| F-AUD-12 | LOW (D124 residual) | Reset-code mail is still sent for a tombstoned account (no session results); `profiles.me` and `account.deletionStatus` remain readable by a still-valid JWT for its lifetime (documented design). | code read (`auth.ts`, `profiles.ts:38`) | `convex/auth.ts`, `convex/profiles.ts` | T18.5 if cheap |

Not registered (already closed and re-verified at ba15aaf): D115 6b-1…6b-8, D112 6a-1…6a-6, D107 C1–C6, D99 N1–N8, F-T22-1, F-T15-1, F-T24b-1/2, F-T24c-1, F-T24d-1 (copy), F-D74-1 (accepted `it.fails`).

---

## 4. Rows whose status changed (register → this audit)

| Row | Register | This audit @ba15aaf | Why |
|---|---|---|---|
| C02 | VERIFIED_LOCAL (dev via seed) | VERIFIED_LIVE (dev, E2E transport) + BLOCKED_EXTERNAL (real mail) | T23 §2/§4 |
| C03 | NOT_IMPLEMENTED | VERIFIED_LIVE (dev isolation e2e) / VERIFIED_LOCAL | implemented since T04; boundary.test 65 |
| C04 | VERIFIED_LIVE (their prod) | VERIFIED_LOCAL + BLOCKED_EXTERNAL (live); finding F-AUD-2; B3 open | live evidence is `d88c5f8` |
| C06 | NOT_IMPLEMENTED | VERIFIED_LOCAL + BLOCKED_EXTERNAL (OpenAI) | intake.test 62 |
| C09 | VERIFIED_LOCAL | VERIFIED_LIVE (dev e2e) | T23 §4 |
| C10 | NOT_IMPLEMENTED | VERIFIED_LOCAL | claims.test D46 |
| C11 | VERIFIED_LIVE (their prod) | VERIFIED_LOCAL + BLOCKED_EXTERNAL (OpenAI); B5 open | `d88c5f8` |
| C13 | VERIFIED_LIVE (their prod) | VERIFIED_LOCAL + BLOCKED_EXTERNAL | `d88c5f8` |
| C14 | VERIFIED_LOCAL | VERIFIED_LIVE (dev e2e) | T23 §4 |
| C16 | pending checkpoint 2 | VERIFIED_LOCAL | followUps/drafts tests |
| C17 | NOT_IMPLEMENTED | VERIFIED_LOCAL | claims.test + repro |
| C18 | NOT_IMPLEMENTED | VERIFIED_LOCAL | drafts.test D52 |
| C20 | VERIFIED_LOCAL | VERIFIED_LIVE (dev e2e) | T20/T23 |
| C21 | NOT_IMPLEMENTED | VERIFIED_LOCAL (+ two routes live) | http.test; T23 §2 |
| C22 | NOT_IMPLEMENTED | VERIFIED_LOCAL + BLOCKED_EXTERNAL (codegen secrets) | T23 §1; CI |
| C23 | NOT_IMPLEMENTED | VERIFIED_LOCAL (+ one live redacted line) | log/errors tests; T23 §3.5 |
| C24 | VERIFIED_LIVE (their prod) | BLOCKED_EXTERNAL (candidate never deployed) | `d88c5f8`; T23 §2 |
| C25 | VERIFIED_LOCAL (F2 open) | VERIFIED_LIVE (dev) + BLOCKED_EXTERNAL (OpenAI) | F2 closed; T23 §3.5/§4 |
| C28 | VERIFIED_LOCAL (F4 open) | VERIFIED_LOCAL + BLOCKED_EXTERNAL | F4 closed (D105) |
| C30 | VERIFIED_LOCAL (F10 open) | VERIFIED_LOCAL | F10 closed (D118) |
| C31 | VERIFIED_LOCAL after T05.1 | VERIFIED_LOCAL (auth loop live on dev via C02) + BLOCKED_EXTERNAL (real mail) | T05.2 landed |
| C32 | NOT_IMPLEMENTED | VERIFIED_LOCAL with T18.5 conditions + BLOCKED_EXTERNAL (AgentMail) | T18–T21 landed |
| C33 | VERIFIED_LOCAL; CI not observed | VERIFIED_LIVE (GitHub run 35625965752) + BLOCKED_EXTERNAL (4 secrets) | observed |
| C34 | PARTIAL | VERIFIED_LIVE (dev `ops:backlog`) with B6 + F-T23-3 open | T22 landed |
| C36 | VERIFIED_LOCAL; e2e in progress | VERIFIED_LIVE (dev e2e) | T20/T23 |

Unchanged: C01, C05, C07 (B2 noted), C08 (B4 noted), C12, C15, C19, C26, C27, C29, C35.

---

## 5. Verdict

**Production-ready release candidate: NO — yes-with-conditions once the list below is closed.** The product invariants hold (16/17 proven; the bounded-reads invariant has one HIGH exception on the Board page), every trust boundary I attacked held (webhook, unsubscribe, export, deletion, sign-in, approval/recipient gate, ledger), and the CI pipeline is green on the candidate. What stops a "yes" today:

1. **F-AUD-1** `purchases.board` throws the 4,096-range limit inside the supported caps (100×50) — the Board page fails for heavy accounts. Fix + regression under `transactionLimits: true`.
2. **T18.5 (D124 B1–B6)** must land on `origin/main` with fail-before/pass-after tests (B4 reproduces here as a claim opened for a deleted user; B3 compounds F-AUD-2's orphaned-inbox residue).
3. **F-AUD-2** `ensureInbox` concurrency: wire `inboxProvision` before the fetch and delete the losing inbox.
4. **F-AUD-5** RUNBOOK §11 `--inline-mutation` (operators cannot follow it during an incident).
5. **F-AUD-6 / T25**: static-deploy the candidate to `adorable-lion-138` and pass `npm run smoke` 7/7 there; one `npx convex export` → `import` into a scratch deployment; release manifest with the exact final revision, the four missing CI secrets named, and the single remaining production action (`cool-oyster-399` deploy, D83 item 6) documented.
6. Relabel the four `d88c5f8`-era VERIFIED_LIVE rows (F-AUD-3) so the register is truthful about what has and has not run live for the candidate.

LOW items (F-AUD-7…12) do not block.

**Live-integration statement (truthful):** *Implementation complete locally; live integration verification incomplete.* The candidate has been exercised live only on `adorable-lion-138` with placeholder `OPENAI_API_KEY`/`AGENTMAIL_API_KEY`, no `ALERTS_INBOX_ID`, no `AGENTMAIL_WEBHOOK_SECRET`, no `SHOPSAVVY_API_KEY`, and no static build: sign-in/verification (E2E transport), claim confirmation, watches, isolation, resilience/a11y (Playwright), the six scheduler entry points, the failed-read path with the real `FIRECRAWL_API_KEY`, the unconfigured-webhook 401, OIDC discovery, and `ops:backlog` are VERIFIED_LIVE there. BLOCKED_EXTERNAL for the candidate: real verification/reset mail, real price-drop alert, real claim email + reconcile + bounce/complaint webhook, signed inbound webhook, OpenAI extraction/classification/drafting, ShopSavvy lookup, AgentMail inbox create/delete + component purge, production deploy/smoke on `cool-oyster-399`, CI codegen-drift and CI Playwright (secrets). The only live evidence for the provider paths is the collaborator's I7a run of `d88c5f8` on 2026-09-20, which predates the auth, notify, drafts, inbound, replies and profiles rewrites.

---

## 6. Gates re-run by this audit (worktree at ba15aaf)

- `npm ci` → 0 vulnerabilities; `postinstall` applied `@agentmail/convex@0.1.0` patch; `node scripts/check-patch.mjs` OK.
- `npx vitest run` → 65 files, 1287 passed, 1 expected fail (`fairness.test.ts:220`), 42.49 s.
- Adversarial: `zz_audit.test.ts` 8/10 pass (2 designed failures = F-AUD-2, D124 B4); `zz_audit_board.test.ts` 1/2 pass (60×50 ok, 100×50 throws = F-AUD-1).
- GitHub Actions 35625965752 (`ba15aaf`, push): checks ✓, e2e ✓ (Playwright skipped, secrets absent), codegen:check SKIPPED.
- Worktree removed after the run; no file under the production checkout was modified; no `git stash`.

---

## 7. Delta pass (`git log ba15aaf..origin/main`)

Re-fetched at finalization: `origin/main` is still `ba15aaf3addd7ddcbfbfa3fd2f85f32cb55243e1` — **the delta is empty**, so every row above refers to `ba15aaf` and no row status changes from the delta pass.

Observed but NOT audited (uncommitted, not on `origin/main`, therefore not part of the candidate): the shared checkout carries T18.5's in-flight working tree (`git diff --stat` = 15 files, +897/−27: `account.ts/.test`, `drafts.ts/.test`, `mailPurge.ts`, `ops.ts/.test`, `policies.ts/.test`, `priceWatch.ts/.test`, `profiles.ts/.test`, `docs/ops/RUNBOOK.md`, `patches/@agentmail+convex+0.1.0.patch`). A read-only grep shows it targets exactly the D124 list: `isTombstoned` now appears in `policies.ts` (2), `profiles.ts` (3), `priceWatch.ts` (4), `drafts.ts` (3) (B2–B5); `ops.backlog` calls `internal.account.stuckDeletions` (B6); the patch adds `purgeOutbound` (B1); RUNBOOK §11 now names `ops:resetRetentionCursor` instead of `--inline-mutation` (F-AUD-5/F-T23-3). When it lands, re-run `zz_audit.test.ts › B4 (D124)` (expected to flip to PASS) and re-check that F-AUD-2's concurrent-`ensureInbox` case is covered by the B3 fix (it is a different race: same user, not deletion). F-AUD-1 (`purchases.board`) is not in that diff.


## Mission 2 connections (C37–C58) — added 2026-09-23 (lead); all NOT_IMPLEMENTED until their tasks land

| ID | Scenario | Planned producer → consumer | Owning task(s) | Status |
|---|---|---|---|---|
| C37 | Upload → owned storage → extraction → candidate facts | `http.ts` POST /evidence/upload → `evidence.finalizeUpload` → extraction action → `facts` | M13, M23 | NOT_IMPLEMENTED |
| C38 | Transaction confirmation → typed records → evidence links | `transactions`/`facts` writers → `evidence` links | M11, M13 | NOT_IMPLEMENTED |
| C39 | Manual incident → confirmed facts → evaluator selection | incident entry → `facts` → `lib/rules/applicable` | M11, M12, M24 | NOT_IMPLEMENTED |
| C40 | First-party source → captured passage → versioned reviewed rule | `docs/rules/sources` → pack module → rules-reviewer → DECISIONS activation | M02 ✓ (capture), pack review task, M12 | NOT_IMPLEMENTED (captures exist) |
| C41 | Jurisdiction/payment/product classification → rule selection | fact snapshot → `lib/rules/applicable` | M12, M21, M22 | NOT_IMPLEMENTED |
| C42 | Fact snapshot + rule version → deterministic evaluation | `snapshot_*` → pack evaluator → `evaluations` | M12 | NOT_IMPLEMENTED |
| C43 | Missing facts → questions → confirmation → re-evaluation | `opportunities` missingFacts → Questions UI → `putFact` → re-evaluate | M11, M12, M15 | NOT_IMPLEMENTED |
| C44 | Evaluation → authority/amount/deadline/source presentation | `opportunities` query → OpportunityCard | M12, M15 | NOT_IMPLEMENTED |
| C45 | Multiple opportunities → overlap → non-duplicated totals | loss components → `recovery.summary` | M12 | NOT_IMPLEMENTED |
| C46 | Selected opportunity → compatible claim/case | `cases.openCaseForOpportunity` → `claims` | M12, M20 | NOT_IMPLEMENTED |
| C47 | Evidence + rule + facts → versioned claim packet | `packets` builders | M20, M21, M22 | NOT_IMPLEMENTED |
| C48 | Packet edit / source / fact change → approval invalidation | binding hash + ENGINE_VERSION → materiality → version bump | M12, M13, M20 | NOT_IMPLEMENTED |
| C49 | Approved claim → channel → truthful submission evidence | email (AgentMail) / `submissions.record` with `requiredChannel` | M13, M20 | NOT_IMPLEMENTED |
| C50 | Notice anchor → deadline → reminder → current-state guard | `lib/deadlines` (obligor-aware) → in-app attention sweep | M12, wave-2 deadline task | NOT_IMPLEMENTED |
| C51 | Exact card product + guide version → benefit evaluation | card-product fact → benefit packs | M30 | NOT_IMPLEMENTED |
| C52 | Product/serial/VIN → recall candidate → confirmed match | recall packs | M32 | NOT_IMPLEMENTED |
| C53 | Program/source refresh → reviewed update → affected opportunities | `scripts/verify-rule-sources.mjs` → review item → re-evaluation sweep | source-verify task, M34 | NOT_IMPLEMENTED |
| C54 | Rule supersession → preserved historical evaluation → safe re-evaluation | `evaluations` append-only → re-evaluation sweep | M34 | NOT_IMPLEMENTED |
| C55 | New claim categories → ledger → confirmed recovery dashboard | scenario claims → `ledgerEvents` → `recovery.summary` | M10, M12, M20 | NOT_IMPLEMENTED |
| C56 | Sensitive-document export/deletion → storage/providers/jobs | `account.ts` export/purge incl. `_storage` blobs | M14 | NOT_IMPLEMENTED |
| C57 | Coverage registry → UI availability → no unsupported automation | production registry (active packs only) → coverage list UI | M12, M15/M35 | NOT_IMPLEMENTED |
| C58 | Diagnostics / feature controls → operator action → bounded recovery | `ops.backlog` + extraction flag + rule-eval/source-refresh logs | wave-2 ops task | NOT_IMPLEMENTED |
