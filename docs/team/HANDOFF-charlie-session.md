# Handoff: Charlie's setup + backend session

Written 2026-09-20 ~16:00 PDT from `/Users/charlie/hackathons/recoup`. All background agents are stopped. Nothing is pushed. This file is uncommitted.

## 1. Convex deployment (connected, running)

- Team `allgas` (id 538278) → project `recoup` → dev deployment **`earnest-setter-354`**.
  - Dashboard: https://dashboard.convex.dev/t/allgas/recoup/earnest-setter-354
  - Client URL `https://earnest-setter-354.convex.cloud`, site URL `https://earnest-setter-354.convex.site`
  - `.env.local` (gitignored) holds `CONVEX_DEPLOYMENT`, `VITE_CONVEX_URL`, `VITE_CONVEX_SITE_URL`.
- This is a **different deployment** from nihalnihalani's `adorable-lion-138` referenced in `CONNECTIONS.md` / `HANDOFF.md`. URLs in those docs are stale for this machine.
- Env vars set on the dev deployment (names only): `OPENAI_API_KEY`, `FIRECRAWL_API_KEY`, `AGENTMAIL_API_KEY`, `AGENTMAIL_WEBHOOK_SECRET` (all real, set by Charlie), `JWKS`, `JWT_PRIVATE_KEY`, `SITE_URL` (generated by `npx @convex-dev/auth`, `SITE_URL=http://localhost:5173`).
- AgentMail webhook **created** in Charlie's AgentMail console: endpoint `https://earnest-setter-354.convex.site/agentmail/webhook`, event `message.received` only (blocked/spam/unauthenticated/envelope deliberately unsubscribed). D35's "secret unset" state no longer applies here.
- HEAD (`1088f83`) is pushed to the deployment: modules auth, purchases, claims, followUps, policies, priceWatch, drafts, replies, inbound, intake, profiles; components agentmail, firecrawl, staticHosting; cron registered.
- Deployment tables are empty (no users yet). One orphan AgentMail inbox exists in Charlie's AgentMail org from the T07 agent's live check of `createInbox` (D32 shape confirmed live: `inbox_id`, `email`).
- The Convex CLI was already logged in. The team access token Charlie pasted in chat was never used; it should be rotated.

## 2. Tooling installed

| Tool | State |
|---|---|
| Convex CLI | 1.46.0 (project dep) |
| Convex plugin | `convex@claude-plugins-official` 1.14.0, user scope, enabled. Bundles Convex MCP server (`npx -y convex@latest mcp start`), connected; agents `convex:convex-expert`, `convex:convex-reviewer` |
| Convex AI files | `convex/_generated/ai/guidelines.md`, Convex sections in `CLAUDE.md` + `AGENTS.md`, all up to date (`npx convex ai-files status`) |
| Convex skills | 33 in `.claude/skills/` and `.agents/skills/`, pinned in `skills-lock.json` |
| Firecrawl | CLI 1.23.3 global, authenticated by Charlie; 28 global skills; claude.ai Firecrawl MCP connected |
| AgentMail | claude.ai AgentMail connector connected (inbox/thread/message/draft tools; no webhook tools). A duplicate local `agentmail` HTTP MCP entry needs OAuth and can be ignored or removed |
| OpenAI | `openaiDeveloperDocs` MCP (no key), local scope |
| Docs | context7 MCP connected; `convex-docs` skill |
| Not working, not needed | `vercel` MCP (needs auth), `github` MCP (401 bad token), oh-my-claudecode plugin (fails to load) |

MCP servers added this session are in local scope (`~/.claude.json` for this project), not a shared `.mcp.json`.

## 3. Code: six local commits on `main`, not pushed

Base: origin/main `4ba507e`. Charlie confirmed nihalnihalani's agent team had stopped before this work began.

| Commit | Task | Contents |
|---|---|---|
| `de279fe` | T04 | `convex/lib/access.ts`, `convex/purchases.ts` (create, confirm, setReturned, remove, get, board) + tests |
| `045c990` | T05 | `convex/claims.ts` (open, openClaim, applyEvent, applyEventInternal, confirmCredit, recordLaterDebit, adjustExpected, dismiss, clearAttention, scheduleClaimReminder, get, needsAttention), `convex/followUps.ts` finished per D28 + tests |
| `d835882` | T08 | `convex/policies.ts` (insertSnapshot, fetchOne, fetchBoth, refresh, confirm, setManual, latestForDomain), `convex/lib/policyText.ts` + tests |
| `aa88b2a` | T09 | `convex/priceWatch.ts`, `convex/crons.ts` (6-hourly) + tests |
| `5cff0ab` | T10 | `convex/drafts.ts`, `convex/replies.ts` + tests |
| `1088f83` | T07 | `convex/profiles.ts`, `convex/inbound.ts`, `convex/intake.ts` + tests |

Verification at HEAD, run by the session lead (not the authoring agents): `npm test` 247 passed / 16 files; `npm run typecheck` clean; `npm run lint` clean except a pre-existing fast-refresh warning in `src/components/Money.tsx`; `npx convex dev --once` succeeds.

All work was instructed to follow `docs/team/DECISIONS.md` D01–D35 over the source plan, and not to edit `convex/schema.ts`.

### Known gaps (be honest about these)
- **No independent review.** The build agents returned empty final reports, so their claims were not read; only tests/typecheck/lint/deploy were checked. No `convex:convex-reviewer` or authz audit pass has run. T13 hardening should treat T04–T10 as unreviewed.
- **No live end-to-end run.** Firecrawl policy fetch, OpenAI extraction/classification/drafting, AgentMail send and inbound webhook have not been exercised live (no signed-in user existed). Only `createInbox` was called live once.
- **The T07 agent was stopped by request** just after its commit and cleanup (temporary `convex/liveCheck.ts` is gone locally and from the deployment). Its commit passes all checks, but it never got to a final self-review.
- `PLAN.md`, `CONNECTIONS.md`, `VERIFICATION.md` statuses were **not** updated (lead-owned per D09).

## 4. Uncommitted working tree (left for the lead to decide)

- Modified: `.gitignore` (adds `ClaimHero/`, `bipolar/` reference clones), `docs/ARCHITECTURE_PATTERNS.md` (edited in an earlier session), `package-lock.json` (from `npm install`), `convex/_generated/api.d.ts` (regenerated, +20 lines — should be committed with the backend).
- Untracked: `.agents/`, `.claude/`, `AGENTS.md`, `CLAUDE.md`, `skills-lock.json`, `convex/_generated/ai/` (Convex AI tooling), `docs/CONVEX_REFERENCE_ANALYSIS.md` (earlier session), `.omc/` (oh-my-claudecode state; probably gitignore), this file.
- `ClaimHero/` and `bipolar/` are shallow reference clones of the two top-scoring Convex architecture projects; gitignored.

## 5. What remains

1. Commit `convex/_generated/api.d.ts`; decide on the tooling files; push (or PR) the six commits.
2. T11b frontend flows against the function names above; T12 example loader; T13 hardening + reviewer/authz pass.
3. Live rehearsal on `earnest-setter-354`: sign up → ensureInbox → paste/forward order → confirm → policy fetch → claim → draft → send to a controlled inbox → reply → promised → confirm credit. The AgentMail connector can read/reply from a controlled inbox.
4. T14–T16 (prod deploy with a second webhook + prod env vars, `hackathon.md`, video, social, submit) need Charlie's explicit authorization. Deadline: 2026-09-22 12:00 PT.
