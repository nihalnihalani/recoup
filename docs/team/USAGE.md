# Usage accounting (orchestration evidence, not billing)

Policy: user's mixed-model usage-control prompt (2026-09-23, §3A): Sonnet 5 builds; Opus 5.5 plans/reviews at checkpoints; ≤ 3 active agents; ≤ 6 Sonnet and ≤ 3 Opus assignments per run; stop new work at 30% remaining or a 15-point drop from the run's start in any window.

## Run 2026-09-23 13:11 IST (lead session, base origin/main 158ab1f)
- Quota source: Claude Code app usage reader (`get_usage`), read 13:11 IST. 5-hour window 1% used (resets 18:09 IST); weekly all-models 27% used (resets 2026-09-28). Stop thresholds: 5-hour ≥ 16% used, or weekly ≥ 42% used.
- Lead: this session runs **Opus 5.5** (claude-opus-5-5, self-reported); the prompt asks for a Sonnet 5 coordinator — switching the session model is the user's choice in the app's model picker. Lead keeps its own work to integration, rulings and the single push.
- Prior state: two usage-limit stops earlier today (08:15 and ~12:40 IST) interrupted every Opus lane; all in-flight work is preserved in durable worktrees (see HANDOFF).
- Selected batch (§0E default): **retention and privacy truth** — land backend-6's re-audit batch B (P07-W2 needs_review payload retention + P07-W3, P12-W2/W4/W7, P12-S-4, P03-B, P03-SK-1, P09-SK-2 backend; built and green, needs re-pin D266) and the frontend P09-F2 truthful provider disclosures (from `recoup-wt-m30ui` WIP), then one fresh Opus audit of privacy truth and the retention/deletion/export connections.
- Assignments: Sonnet #1 (integrate batch B + P09-F2, gates, one local commit) — done, model requested "sonnet", self-reported claude-sonnet-5; local commit 682d10a (26 files; vitest 192 files, 4,224 pass + 1 expected fail + 1 todo; typecheck ×2, lint 0, build, check-rule-packs OK). Usage after it: 5-hour 2% used, weekly 27%. Opus #1 (fresh independent audit of 682d10a) — done, self-reported claude-opus-5-5: approve_with_changes (F1 retention×pause retry on empty text, F2 durable-pause copy, F3 four privacy strings; notes: credit margin, D266 wording, RUNBOOK test name); 404/404 targeted tests re-run by the auditor. Sonnet #2 (same worker: F1–F3 + notes, amend, full gate) — dispatched. Opus #2/#3 unused (the lead verifies the low-severity fixes against the finding list instead of a second Opus pass).

- Batch 2 (test determinism): Sonnet #3 (same worker) — local commit 8ed290c, full gate green three times; lead spot-check 83/83 (guard, completeness, alerts.flow, account). No Opus audit (low-risk test infrastructure; folded into the next audited batch). Usage before push: 5-hour ~5%, weekly 28%.
