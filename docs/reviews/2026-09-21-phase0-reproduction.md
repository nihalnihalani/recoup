# Phase 0 reproduction summary — main e521de9 (2026-09-21)

States judged by Sonnet reproducers and checked by two Opus refuters each. P09 reproducer failed (structured-output formatting); re-run separately.

| Item | Claim | State | Severity | Evidence (first 160 chars) |
|---|---|---|---|---|
| P01 | No email verification before automatic alert delivery | still_present | high | convex/auth.ts:1-5 calls `Password` with no config object at all (`providers: [Password]`), so `config.verify` is undefined. In node_modules/@convex-dev/auth/sr |
| P01 | No password recovery | still_present | high | convex/auth.ts:1-5 passes no `reset` option to `Password`. node_modules/@convex-dev/auth/src/providers/Password.ts:166-169 throws `Password reset is not enabled |
| P01 | Verification/preference not rechecked at send time | still_present | high | convex/notify.ts's `dropContext` (lines 194-242) does freshly re-read `watch.status` (line 214) and `user.email` (line 206) at send time — but there is no verif |
| P01 | No opt-out/unsubscribe | still_present | high | convex/schema.ts's `profiles` table (lines 39-40) has only `userId`, `inboxId`, `inboxEmail`; authTables' `users` (node_modules/@convex-dev/auth/dist/server/imp |
| P01 | Suppressed emails not preserved as in-app drops with reason | fixed | low | convex/notify.ts `claimDrop` (lines 151-167) always inserts a `mailLog` row and sets `status: "failed"`, `error: limitError` when the daily/global cap is hit. ` |
| P01 | Changing address does not invalidate verification | not_reproduced | info | No mutation anywhere in convex/*.ts patches `users.email` after account creation outside of test helpers (`grep -rn "db.patch(user\/patch(userId" convex/*.ts` a |
| P01 | UI copy does not distinguish per-message merchant approval from automatic alerts | still_present | medium | src/pages/SignIn.tsx line 19 shows every signed-out visitor the unqualified promise: "You approve every message before it is sent. Recoup never files claims in  |
| P02 | In notify.sendDrop, the context read, the component enqueue, markQueued, and reconciliation scheduling really  | still_present | info | convex/notify.ts:304-344. sendDrop (an internalAction) makes three independent calls: ctx.runQuery(internal.notify.dropContext) at 312, agentmail.sendMessage(.. |
| P02 | A crash after the enqueue but before persisting outboundId can duplicate or lose the send ('plain action read  | still_present | high | convex/notify.ts:199 (dropContext's only re-entry guard is `row.status !== "claimed" -> return null`) and notify.ts:329-341 (the try/catch around markQueued/fin |
| P02 | Simultaneous invocations can duplicate the CLAIM step itself (two concurrent recordWatchCheck/claimDrop calls  | not_reproduced | low | claimDrop runs as a plain function call inside recordWatchCheck's own transaction (convex/watches.ts:713, inside the internalMutation starting at watches.ts:634 |
| P02 | Reconciliation stops after bounded backoff, leaving rows queued with no next check. | still_present | medium | convex/notify.ts:383-391 (applyDropOutcome: once `attempt >= BACKOFF_MS.length` it returns 'unknown' and schedules nothing further, leaving mailLog.status as 'q |
| P02 | No manual recheck exists for a row stuck at 'queued' or 'claimed' with no automated next check. | partially_present | medium | For merchant drafts this is FIXED: convex/drafts.ts:591-602 exports `recheckSend` (D56, 'lets the owner ask for one more delivery check on demand'), tested at d |
| P02 | accepted / sent / delivered / bounced are conflated into one status. | still_present | medium | The real AgentMail component distinguishes 7 states (node_modules/@agentmail/convex/dist/component/shared.js:2: pending/sent/failed/delivered/bounced/complained |
| P02 | Apply the same claims to merchant drafts (drafts.approveAndSend/reconcileSend) without regressing newest-draft | fixed | info | drafts.approveAndSend is structurally different from notify.sendDrop in exactly the way that matters: it is one mutation, so the component enqueue (drafts.ts:46 |
| P03 | recordSnapshot sets marketFetchedAt even when the key is missing or the provider fails, and market.refresh ref | still_present | high | convex/market.ts:106-121 recordSnapshot unconditionally does `await ctx.db.patch(watchId, { marketFetchedAt: now, marketNote: note })` regardless of outcome. co |
| P03 | No not_configured/queued/running/success/empty/retryable/terminal state distinction | still_present | medium | convex/schema.ts:89 the only persisted state is `marketFetchedAt: v.optional(v.number())` plus `marketNote: v.optional(v.string())` — a timestamp-or-not plus a  |
| P03 | No transactional claim before the paid lookup, so simultaneous manual refresh and automatic checks can duplica | still_present | high | The only 'claim' anywhere is the read-check `watch.marketFetchedAt === undefined` performed independently in three places that never themselves write marketFetc |
| P03 | No capped retry/backoff | still_present | high | market.lookup (market.ts:169-241) never lets fetchSnapshot's errors propagate — it catches them (market.ts:182-186) and turns them into a note, so the action al |
| P03 | No migration for old failed stamps | still_present | medium | `find convex -iname '*migrat*'` returns nothing relevant; the only text hit for 'migrat' in convex/ is a comment in convex/examples.ts:23 ('there is no migratio |
| P03 | No market.test.ts orchestration tests | still_present | medium | A filesystem search for market.test.ts finds nothing anywhere in the repo. grep -li market across all *.test.ts files finds only convex/lib/shopsavvy.test.ts (p |
| P04 | Median outlier filtering does not prove variant/condition/bundle/shipping/currency match | still_present | medium | convex/lib/shopsavvy.ts:157-166 `withoutOutliers` is a pure price-band heuristic (median*0.4..median*1.75) with no field of the offer other than `cents` consult |
| P04 | Unknown currency handling is undefined | still_present | medium | convex/lib/shopsavvy.ts:178 `if ((point.currency ?? currency) !== currency) continue;` and shopsavvy.ts:213 `if ((offer.currency ?? currency) !== currency) cont |
| P04 | Stale-listing handling is undefined | partially_present | low | No staleness threshold exists anywhere in convex/lib/shopsavvy.ts or convex/market.ts — an offer's own `observedAt` (which the code's own doc comment at shopsav |
| P04 | Historical provider data could independently create claims or alerts | not_reproduced | info | Traced every call site: `openClaim` is called only from convex/examples.ts:115,198 (seed/demo data), convex/claims.ts:144 (user-initiated return_credit), convex |
| P04 | Confirmed offer matching may silently authorize later variants | partially_present | medium | Two real guards exist and are exercised: (1) convex/offers.ts:551-556 `recordCandidates` only refreshes a `confirmed` row's price when the NEW candidate's `prod |
| P04 | Provider envelopes/size/minor units/timestamps/URLs not validated at the boundary | partially_present | medium | Mixed by sub-claim. Envelope shape: convex/lib/shopsavvy.ts:102-107 `parseSnapshot` does check `typeof body === object`, array/object `data`, and drops the whol |
| P04 | (Consequence of the timestamp gap above) verdict()'s market fallback does not filter future/corrupted timestam | still_present | medium | convex/lib/verdict.ts:127-128 filters the watch's OWN `history` with `h.observedAt <= now`; convex/lib/verdict.ts:151 passes `input.market` into `fromMarket()`  |
| P04 | Missing ShopSavvy credentials must not break core watching | fixed | info | convex/market.ts:49-51 `fetchSnapshot` returns `null` immediately (no throw) when `process.env.SHOPSAVVY_API_KEY` is unset. `lookup` (market.ts:169-186) treats  |
| P04 | (Related, not in the harness's 7-item list, but directly relevant to the same market.ts boundary and matches a | still_present | medium | convex/market.ts:78 `watchForMarket` returns null (skips the lookup) once `watch.marketFetchedAt !== undefined`; convex/market.ts:121 `recordSnapshot` unconditi |
| P05 | Archive churn hides active data — userWatches takes 40 newest before archived filtering; userPurchases takes 4 | still_present | high | convex/insights.ts:86-92 `userWatches()` does `.query("watches").withIndex("by_user", userId).order("desc").take(MAX_WATCHES=40)` with NO status predicate; ever |
| P05 | Double counting — a watch marked bought increments both the bought-watch tally and the purchase item created f | still_present | high | convex/insights.ts:304-308: for each live (non-archived) watch, `if (watch.status === "bought") target.bought += 1;` keyed on `watch.merchantDomain` (L306). Sep |
| P05 | Money summed/compared across currencies | still_present | medium | convex/insights.ts's `tally()` (L282-302) and the offers loop (L316-332) update `target.bestCents` by pure numeric comparison of `observedCents`/`lastCents` (L2 |
| P05 | Sampled totals reported as account totals | still_present | high | Every count in sourceRow (checks, priced, drops, bought, watching, offers — schema at insights.ts:242-258) is computed over a hard-bounded, silently-truncated s |
| P06 | Foundational premise: watches.list/get bake Date.now() into reactive query summaries | still_present | info | convex/watches.ts:269 (`list`: `const now = Date.now();`) and convex/watches.ts:289 (`get`: `summarise(watch, checks, Date.now(), ...)`) both snapshot the wall  |
| P06 | Countdowns (next-check timer, 'last checked X ago') go stale in an open tab | not_reproduced | info | `nextCheckAt` and `lastCheckedAt` are returned as raw epoch-ms fields (watches.ts:87-88, 203-204), never pre-formatted with `now` baked in. The frontend recompu |
| P06 | Search leases (offer-search cooldown/pending state) go stale in an open tab | still_present | medium | convex/offers.ts `listForWatch` (a query) snapshots `const now = Date.now()` at offers.ts:358 and derives two booleans/gates from it at write time: `searching`  |
| P06 | Cooldowns (watch 'Check now' cooldown / checking indicator) go stale in an open tab | partially_present | medium | The mutation-side eligibility check IS fresh and authoritative and does not have this problem: `checkNow` (watches.ts:380-400) and `find` (offers.ts:268-296/241 |
| P06 | Verdict age (how stale is the price behind the verdict) goes stale/blind in an open tab | still_present | medium | The root cause here is not really query-caching but a data-modeling gap: `verdict()` (convex/lib/verdict.ts:120-190) only uses its `now` argument to filter out  |
| P06 | Stale last-known prices are shown without qualification | still_present | medium | `watch.lastCents` (convex/schema.ts:84-92) is set once on acceptance (watches.ts recordWatchCheck, `patch.lastCents = observedCents` only when `observedCents != |
| P06 | Last successful observation is not shown separately from last attempt | still_present | medium | `watchSummary` (watches.ts:76-128) has exactly one 'when' field for check activity, `lastCheckedAt`, and `recordWatchCheck` (watches.ts:687-689) stamps it to `n |
| P06 | Failed reads make old data look fresh (the P06 acceptance criterion: 'days of failed reads cannot show an unqu | still_present | high | Direct consequence of the three findings above: `lastCheckedAt` is bumped by a failed check (looks fresh), `lastCents`/verdict are unchanged (are actually stale |
| P07 | Nested per-product reads across insights.ts dashboard queries | still_present | medium | convex/insights.ts: `activity` (104-240) does, per non-archived watch (up to MAX_WATCHES=40), a nested `watchChecks` query (116-120); per active purchase (up to |
| P07 | Serialized (non-parallel) fan-out over per-product reads | partially_present | low | Still sequential (a `for` loop awaiting inside the loop body, no `Promise.all`): convex/insights.ts `activity` (112, 124, 176, 195, 204, 218 — every per-watch/p |
| P07 | Unbounded .collect() calls | partially_present | low | Two different situations exist under the same code pattern. (a) Transitively bounded despite no `.take()` on the read itself: `insights.ts:137` (`items` by_purc |
| P07 | Possible transaction-limit failures | not_reproduced | info | Under the code's own stated per-parent maximums (insights.ts MAX_WATCHES/MAX_PURCHASES/MAX_CLAIMS=40, limits.ts MAX_ITEMS_PER_PURCHASE=50, insights.ts CHECKS_PE |
| P07 | No per-user fairness when global provider budgets are exhausted | still_present | medium | convex/watches.ts `sweep` (739-763): reads one global page (WATCH_SWEEP_PAGE=50) off `by_status_nextCheck` ascending across ALL users, then trims it to whatever |
| P07 | No backlog visibility when global budgets are exhausted | still_present | medium | Grepped convex/*.ts and the DB schema for any backlog/queue-depth surface: none exists. `watches.ts` `list`/`get` (watchSummary, 76-128) expose `nextCheckAt` an |
| P07 | No retention/archive policy for scrape bodies, email payloads, price history, and operational events | still_present | medium | convex/schema.ts (33-199): no table (`watchChecks`, `priceChecks`, `offerChecks`, `marketPrices`, `mailLog`, `processedEvents`, `usage`, `claimNotes`) has a TTL |
| P08-boundary-authz-audit | Possible foreign-ID acceptance on public ACTIONS (drafts.generate, intake.paste, policies.refresh, profiles.en | not_reproduced | info | Inventory of all public actions found exactly 4: drafts.ts:209 generate (args: claimId), intake.ts:607 paste (no ids), policies.ts:274 refresh (merchantDomain s |
| P08-boundary-authz-audit | URL validation limits: DNS rebinding is not blocked by string/hostname checks. | still_present | low | lib/watchUrl.ts:parseProductUrl (convex/lib/watchUrl.ts:25-45) rejects non-http(s) schemes, embedded credentials, non-default/non-80/443 ports, literal loopback |
| P08-boundary-authz-audit | Provider egress: URLs handed to Firecrawl/ShopSavvy from provider-origin data (search hits, ShopSavvy store li | fixed | info | lib/offerMatch.ts:cleanStoreUrl (convex/lib/offerMatch.ts:49-60) calls parseProductUrl internally and is used uniformly for: (a) Firecrawl search hits in offers |
| P08-boundary-authz-audit | Inbound webhook: signature verification. | fixed | info | convex/http.ts:12-26 mounts the AgentMail component's handleWebhook at POST /agentmail/webhook. node_modules/@agentmail/convex/dist/client/index.js:137-155 (han |
| P08-boundary-authz-audit | Inbound webhook: replay keys / idempotency. | fixed | info | Two independent layers dedupe the same webhook delivery: (1) component layer -- node_modules/@agentmail/convex/src/component/lib.ts:433-445 handleEvent mutation |
| P08-boundary-authz-audit | Inbound webhook: sender association (a message can never be attached to another user's claim/inbox). | fixed | info | convex/inbound.ts:onMessageReceived resolves the owning user strictly from the receiving inboxId -> profiles.by_inbox (inbound.ts:124-129), and findReplyClaim ( |
| P08-boundary-authz-audit | Inbound: sender association / trust-labeling -- sameParty() in replies.ts treats an unparseable sender address | still_present | low | convex/replies.ts:38-42: function sameParty(a, b) { if (!a // !b) return true; ... }. emailDomain() (convex/drafts.ts:87-94) returns null for any address with n |
| P08-boundary-authz-audit | Inbound / draft-writing: prompt injection from untrusted content (scraped pages, inbound emails) reaching the  | fixed | info | convex/lib/ai.ts:extract puts all untrusted content only in the user role and appends a fixed instruction to the system prompt: 'The user message contains untru |
| P08-boundary-authz-audit | Inbound / intake: secret/PII redaction on failure paths shown to users. | fixed | info | convex/lib/errors.ts:sanitizeError maps any raw error string into one of 4 generic buckets ('Extraction failed' / 'Provider error' / 'Invalid data' / 'Something |
| P08-boundary-authz-audit | (args/returns line item of the P08 checklist) Every exported public Convex function should have both an args v | partially_present | medium | Args validators are present everywhere (spot-checked across all files). Returns validators, however, are inconsistently applied: convex/claims.ts has ZERO retur |
| P08-boundary-authz-audit | (P08 acceptance bar: 'signed/unsigned/duplicate webhook contract tests') Test coverage for the actual HTTP web | still_present | medium | There is no convex/http.test.ts (confirmed: ls convex/http.test.ts -> no such file) and no test in convex/*.test.ts issues an HTTP request against the /agentmai |
| P10 | Vitest include is convex/**/*.test.ts; no checked-in browser suite exists. | still_present | high | vitest.config.mts: test.include = ["convex/**/*.test.ts"]. package.json test/test:watch scripts unchanged. No playwright/cypress in package.json; no *.spec.ts o |
| P10 | No E2E for auth, watch create/check/pause, conversion, policy review, draft approval, ledger confirmation, unk | still_present | high | Zero browser/DOM-level tests exist for any of these flows. This is not an absence of ALL verification: convex/watches.test.ts, drafts.test.ts, policies.test.ts, |
| P10 | Unknown IDs — direct navigation to /purchases/:id or /claims/:id with a bad or another user's id. | still_present | high | convex/lib/access.ts:14-19 (ownedPurchase) and :26-29 (ownedClaim) throw new ConvexError(...) for a missing or foreign-owned id. src/pages/Purchase.tsx:300 and  |
| P10 | Accessibility/focus/error boundaries unverified. | partially_present | medium | No automated accessibility verification exists: no axe-core/jest-axe/@testing-library in package.json, no browser suite. Manual accessibility work is real and n |
| P10 | 552 kB main chunk. | still_present | low | Fresh `npm run build` today produces dist/assets/index-DO8pNFU8.js at 552.30 kB / gzip 156.17 kB — matching the baseline's 552.30 kB to the byte, i.e. no code-s |
| P10 | No offline/reconnect handling. | still_present | medium | No code anywhere surfaces Convex's websocket connection state or navigator.onLine to the UI. |
| P10 | Fault/race window: notify.sendDrop's provider call and its write-back to the mailLog row are two separate, non | still_present | high | convex/notify.ts sendDrop (internalAction) calls agentmail.sendMessage(...) and only afterward, in a second try block, calls ctx.runMutation(internal.notify.mar |
| P10 | Direct-route refresh (hard reload on a deep link like /watching or /purchases/:id). | not_reproduced | low | The deploy path (npm run deploy -> npx @convex-dev/static-hosting deploy, no --no-spa flag) uses @convex-dev/static-hosting's default SPA fallback ('paths witho |
| P11 | Test script uses --passWithNoTests, so a future empty/misconfigured suite would exit 0 instead of failing CI. | still_present | low | package.json:11 — "test": "vitest run --passWithNoTests". vitest.config.mts:7 scopes collection to include: ["convex/**/*.test.ts"] only (no src/ frontend test  |
| P11 | Lockfile/generated Convex API consistency is unverified from a clean checkout. | still_present | medium | No .github/ directory exists (confirmed) and no CI config of any kind exists outside node_modules. This exact failure mode bit the team before: docs/team/VERIFI |
| P11 | postinstall runs patch-package, but patch-package is only a devDependency, so behavior under a production inst | still_present | high | package.json:14 `"postinstall": "patch-package"`; package.json:41 lists `"patch-package": "^8.0.1"` under devDependencies only. The patch it applies, patches/@a |
| P11 | Dependency audit has not been done. | still_present | high | No file under docs/ mentions 'npm audit', a CVE, or any dependency-vulnerability review. Running `npm audit --omit=dev` in the clean worktree surfaces one CRITI |
| P11 | (Instruction-mandated check) whether any P11 claim is a race/fault-window bug convex-test could exercise. | not_reproduced | info | None of the four P11 claims are runtime concurrency/fault-window bugs inside Convex functions — they are install-tooling and CI-process gaps (npm script flags,  |
| P12 | Deployment identity/commit/build id/config presence is not recorded anywhere reproducible. | still_present | medium | package.json:4 version is hardcoded "0.0.0" and never bumped. vite.config.ts has no `define` injecting a commit SHA or build id. Repo-wide grep for build-id/com |
| P12 | README's live-status claim directly contradicts its own feature table two paragraphs later. | still_present | medium | README.md:16 states 'Price-drop emails, "I bought it" on a watched item, and the same item at other stores are in the UI and have run end to end on production.' |
| P12 | README's cron cadence description is stale relative to convex/crons.ts. | still_present | low | README.md:77 says '...six-hour price watch, hourly watch sweep...' but convex/crons.ts:13 registers crons.interval("price watch", { hours: 2 }, internal.priceWa |
| P12 | README's decision-range reference is stale. | still_present | low | README.md:89 says 'docs/team/DECISIONS.md / Decisions D01 to D49; these override the original plan.' The current file runs through D63 (docs/team/DECISIONS.md:8 |
| P12 | Missing docs for SHOPSAVVY_API_KEY, ALERTS_INBOX_ID, and APP_URL. | still_present | medium | README.md's env-var table (lines 54-60) lists only OPENAI_API_KEY, FIRECRAWL_API_KEY, AGENTMAIL_API_KEY, AGENTMAIL_WEBHOOK_SECRET, and the auth trio. All three  |
| P12 | Missing docs for AI model selection and webhook/signing setup. | partially_present | low | AI model: actually documented — hackathon.md:56 names the model ('gpt-5.6-terra', convex/lib/ai.ts:6) and explains the money-safety guarantee around model outpu |
| P12 | No correlation IDs, and operational-signal redaction is real but narrow (covers only inbound/intake extraction | partially_present | medium | A genuine, unit-tested redaction function exists: convex/lib/errors.ts:16 sanitizeError, used at convex/intake.ts:126,160 and convex/inbound.ts:185 (D58) to tur |
| P12 | No service targets or smoke checks. | still_present | medium | No health-check route (grep for health.?check/healthz across convex/ and src/ found nothing but unrelated substring matches), no SLO/latency-target constants, n |
| P12 | No operator kill-switch documentation. | still_present | high | A real fail-closed/budget mechanism exists in code (convex/lib/budget.ts: per-user and 'deployment-wide kill switch' rows with no userId, capped by hardcoded co |
| P12 | No backup/restore/migration/rollback runbook. | still_present | high | grep -rniE 'backup/restore/migrat/rollback' across docs/team/*.md, README.md, hackathon.md, and convex/*.ts (excluding test-file vi.restoreAllMocks()/mockRestor |

## Refuter dissents (accepted into the plan)
### refute:P07:security
1. Subfinding 'Possible transaction-limit failures': state not_reproduced -> REPRODUCED; severity info -> HIGH. convex/priceWatch.ts:173-189 (eligibleItems: table-wide .take(SCAN_LIMIT=500) then per item db.get purchase + lib/latestPolicy.ts:20-25 .take(20) + priceWatch.ts:122-129 hasOpenPriceClaim .collect()). Empirical: convexTest({schema, modules, transactionLimits:true}) with 500 items x (1 open price_adjustment + 64 dismissed return_credit claims) -> eligibleItems throws 'Scanned too many documents (limit: 32000)'; priceWatch.ts:475 runAll therefore throws every tick for all users. Rows are user-writable without budget via purchases.ts:196-207 setReturned, claims.ts:121-165 open (D44 allows re-open after dismiss, claims.ts:56-66), claims.ts:301 dismiss; the open price claim per item is produced by the cron/checkNow against a self-hosted drop page (priceWatch.ts:225-311). No deletion path exists to recover. Same as unfixed M6 in docs/reviews/2026-09-20-prelaunch-security-review.md:175-181. Note the reproducer's convex-test call must use the options-object form (convexTest({schema, modules, transactionLimits:true})); the positional form silently ignores the option (node_modules/convex-test/dist/index.js:1858-1862).
2. Subfinding 'No per-user fairness': severity medium -> HIGH for convex/priceWatch.ts (keep medium for watches.ts:739-763 sweep and intake.ts:766-834 retryFailed). priceWatch.ts:180 orders items by _creationTime desc with no item-level nextCheckAt and no rotation after a check (checkRequestedAt at priceWatch.ts:530 is never read by eligibleItems), so the newest <=50 eligible items monopolise every tick permanently, not for one tick; empirically confirmed across tick 1, tick 2 (after priceChecks recorded) and +30 days. watches.sweep by contrast rotates (watches.ts:752-756 bumps nextCheckAt so scheduled rows leave the by_status_nextCheck page; confirmed the starved user is scheduled on tick 2). This is fix 3 of prior H1 (docs/reviews/2026-09-20-prelaunch-security-review.md:78-93) still open; the cron charges only the global switch (budget.ts:31-38, priceWatch.ts:478), so one account takes 600 of 3,000 daily price_check units with no per-user charge. Cadence is 2h (convex/crons.ts:13), not 6h (stale comments priceWatch.ts:4 and :466).
3. Subfinding 'Unbounded .collect()': evidence correction - 'growth is slow because daily budgets throttle draft/reply/retry creation' is false for (a) claims.ts:236-251 confirmCredit/recordLaterDebit (public, unbudgeted ledger appends; self-only, low) and (b) replies rows + processedEvents rows/payloads created per inbound email by inbound.ts:150-166 -> replies.ts:126-160 classifyOnce with no budget (externally driven). MISSING sixth collect: claims.ts:382-384 components.agentmail.lib.listInboundMessages({threadId}) -> node_modules/@agentmail/convex/src/component/lib.ts:411-416 .collect() of every inbound message on the thread, each stored with text/html/extractedText/extractedHtml plus raw (lib.ts:457-

### refute:P08:security
1. Subfinding "secret/PII redaction on failure paths" -> state: partially_present (not fixed), severity: low (not info). convex/intake.ts:847 (attentionRow includes raw `lastError`), convex/intake.ts:877 (handler strips only payload/processingStartedAt), convex/intake.ts:149-163 (failEvent stores raw error, sets no `summary`), convex/replies.ts:100 (`Could not read the reply: ${message}` raw OpenAI text into lastError), src/pages/Settings.tsx:215 (renders `event.summary ?? event.lastError`), convex/intake.test.ts:900-913 (test asserts lastError is returned). Related same-tenant raw provider strings also reach the client: convex/drafts.ts:525 (sendError = component errorMessage, which is `${err.message}: ${err.body}` with up to 4 KiB of AgentMail response body, node_modules/@agentmail/convex/src/component/lib.ts:279 + utils.ts ERROR_BODY_LIMIT), surfaced via drafts.sendStatus convex/drafts.ts:609-628 and drafts.listForClaim/claims.get; convex/notify.ts:322 (`Send failed: ${err.message}`) surfaced via notify.drops convex/notify.ts:450. No secret confirmed in any of these; all owner-only.

2. MISSING (add, severity medium): unauthenticated, unbudgeted OpenAI spend via inbound mail. convex/inbound.ts:154-166 (schedules internal.replies.classify / internal.intake.processEvent for every verified event), convex/intake.ts:115-137 (beginEvent: no charge), convex/intake.ts:192-197 (extract on up to MAX_TEXT_CHARS=60_000, inbound.ts:9), convex/replies.ts:80-100 + :112 (classify: extract with no charge, 2 retries), convex/limits.ts:80-98 (GLOBAL_DAILY_BUDGETS has no intake/reply kind), charge() only at convex/intake.ts:654 (paste) and :711/:734 (retryEvent). Compare docs/reviews/2026-09-20-prelaunch-security-review.md:62-75 (B5, BLOCKER for the authenticated paste path) and :248-264 (spend table that also omits the inbound path). Invariant 16.

3. MISSING (add, severity info): inventory omits convex/auth.ts:4 -- `signIn` (public action), `signOut` (public action), `isAuthenticated` (public query); `store` is internalMutationGeneric (node_modules/@convex-dev/auth/dist/server/implementation/index.js:253, :290, :300, :310; password attempts rate-limited via authRateLimits, dist/server/implementation/rateLimit.js). Public action count is 6, not 4. P08 asks for these to be identified as framework-protected exceptions; acceptance bar "endpoint inventory with no unexplained public mutator" is not met by omission.

4. MISSING (add, severity info): "inbound body limits" line item unaddressed. node_modules/@agentmail/convex/dist/client/index.js:139 (`req.text()` with no app-side cap; only the Convex platform HTTP-action body limit applies), node_modules/@agentmail/convex/src/component/lib.ts:447-473 (stores full `raw: event` plus inboundMessages text/html; an email over the 1 MiB document limit makes handleEvent throw -> HTTP 500 -> provider retries -> the event never reaches processedEvents, i.e. a silent drop with no received/failed row, invariant 10). App-side 60_0
