/**
 * M22 (wave 2): air-travel (R02/R04) keys. An empty stub in wave 1 so `lib/facts/catalog.ts` already merges this file; the owner appends
 * specs here (`as const satisfies readonly FactSpec[]`) and they join `FactKey` automatically. Keys are never renamed.
 */
import type { FactSpec } from "./catalog";

export const AIR_FACT_SPECS = [] as const satisfies readonly FactSpec[];
